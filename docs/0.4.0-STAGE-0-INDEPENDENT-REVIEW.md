# WritCraft 0.4.0 阶段 0 独立复审记录

> 日期：2026-08-06
> 路线图：`WRC-0.4.0-R1`
> 合同：`WRC-EVIDENCE-DELIVERY-V1`
> 复审结论：**P0=0、P1=0、P2=4；阶段 0 可以退出，阶段 A 可以开始**

## 1. 复审范围

本轮只审查阶段 0 的合同、当前源码/测试基线、真实运行记录和活动文档治理。阶段 0 没有修改 `v0/src/`、`v0/tests/`、脚本、`v0/package.json` 或 `v0/npm-shrinkwrap.json`；因此本签字不表示任何 0.4.0 App 能力已经实现。

复审以 [`EVIDENCE-DELIVERY-V1-CONTRACT.md`](EVIDENCE-DELIVERY-V1-CONTRACT.md) 为主要对象，并核对 [`ROADMAP-0.4.0.md`](ROADMAP-0.4.0.md)、[`ROADMAP.md`](ROADMAP.md)、[`INDEX.md`](INDEX.md)、[`../v0/DEVELOPMENT-STATUS.md`](../v0/DEVELOPMENT-STATUS.md)、PRD、README、根 `AGENTS.md` 与冻结兼容合同的派工边界。

## 2. 保留的审查轨迹

阶段 0 先完成一次只读合同审计，结论为 P0=0、P1=10、P2=3；问题写回合同后，由独立复审员持续对同一冻结稿定点复核。六轮结论依次为：

1. P0=0、P1=7、P2=3；
2. P0=0、P1=5、P2=4；
3. P0=0、P1=5、P2=4；
4. P0=0、P1=3、P2=4；
5. P0=0、P1=2、P2=4；
6. **P0=0、P1=0、P2=4**。

最终一轮确认 Snapshot bundle 与对象身份、能力签发链、Markdown 选择性恢复、删除隔离、exact-snapshot 导出、五类引用健康项、共享 Citation/Graph identity、真实图片解码、DOCX/OOXML、Main-only 保存路径、原生 no-clobber 和 committed reconciliation 均已达到可实现的 exact contract。`bindingId`、`entryBindingDigest` 与 macOS `fsid_t` 有符号编码的最后歧义已清除。

## 3. 阶段 0 运行基线

- focused baseline：History 14/14、Diagnostic Export 13/13、SourceIndex 10/10、Citation 10/10、Graph Filter 17/17、Graph Workbench 14/14、Image Trash 21/21、PDF extraction timeout 3/3。
- 完整 `npm test`：退出码 0。
- 首次沙箱 `npm run verify`：在 DOM sanitizer 真实 Electron 探针以 `Electron exited null` 红灯退出；相同命令在获批 GUI 环境退出码 0。两条结果同时保留。
- 获批 GUI 环境 `WRITCRAFT_E2E_FORCE=1 npm run e2e:electron`：连续两次在前 6 个阶段通过后，于“普通 Markdown 可见回收区列表”等待处同点红灯，并同时报告 watcher unavailable / `HOME_SNAPSHOT_TIMEOUT`。独立 watcher Main/IPC 真实 Electron 专项 3/3 通过。
- Computer Use：从 Pages 真实 About 窗口确认 `Pages 14.4 (7043.0.93)`；隔离 WritCraft profile 的 AX app state 连续超时 `-10005`，本轮创建的隔离进程/profile 已清理。
- 工具基线：macOS 26.5.1（25F80，arm64）、Node 26.3.1、npm 11.16.0、Electron 43.2.0。

以上只是阶段 0 基线，不是 0.4.0 自动化、真实 Electron、真实作者或候选通过证据。

## 4. 最终 P2

1. **沙箱 Electron 环境差异**：DOM sanitizer 探针在沙箱内红灯、获批 GUI 环境完整 `verify` 通过；后续每阶段仍需按边界保留首红并在批准环境复核。
2. **大型 Electron harness 同点红灯**：完整 harness 两次在同一回收区等待点失败，尚未解释 watcher/首页 snapshot timeout 的跨组件时序；进入阶段 A 后如触及该路径，必须先分类为产品、harness 或环境问题。
3. **Computer Use AX 超时**：Pages exact 版本已由真实 UI 读取，但 WritCraft 隔离 profile 的 AX app state 读取超时，不能冒充 WritCraft Computer Use UI 通过。
4. **恶意同 UID 主动篡改残余**：V1 将私有 snapshot store 绑定为 `0700`，并使用 fd、身份摘要、隔离区和事务重检；但已控制同一用户账户、可主动篡改私有目录的进程属于主机账户攻陷残余，不能由随机名或事后 `stat` 宣称关闭。

## 5. 放行与未授权事项

独立复审确认阶段 0 的 P0/P1 已清零，允许按路线图进入阶段 A。阶段 A 必须从 Main 项目快照、比较、Markdown 选择性恢复和安全删除权威开始，并重新建立实现、专项、完整自动化、真实 Electron/Computer Use 与独立复审证据。

本签字不授权 npm 发布、dist-tag 变更、GitHub Release/Tag、远端 push、App/ZIP 分发或公开仓库状态变更。
