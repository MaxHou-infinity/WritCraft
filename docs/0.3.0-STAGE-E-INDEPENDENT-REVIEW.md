# WritCraft 0.3.0 阶段 E 独立复审（已签收）

> 复审日期：2026-08-05
> 路线图：WRC-0.3.0-R1
> 状态：**独立复审已签收：P0=0、P1=0、P2=3；阶段 E 证据门禁完成；正式发布仍需另行授权**

## 当前结论

阶段 E 的两条真实作者 Electron 路径现已通过（`author-cross-entry` 1/1、`author-affected` 1/1）。作者选定目录含私有 `.writcraft/author-acceptance-copy.json`，直接重复 copy 的首红为 `COPY_MANIFEST_CONFLICT`；未改源目录，测试仅在临时 staging 排除该私有防嵌套标记后再走生产 copy transaction。真实作者路径现覆盖 Chat、Navigation、Chapter、Research、普通 Changes、Graph、图片、入口取消、Chat 60 秒硬超时、跨项目 A→B 迟到结果丢弃、五入口 edit.md revision 漂移旧结果丢弃、来源充足/不足恢复、Diff/冲突/接受/Safe Undo 和 Markdown 零写入。五入口统一 Context Manifest v2 已有 exact envelope、UTF-8 字节、预算、遗漏/截断/不可用原因、revision 和相对路径不泄露专项与作者对照；当前 `npm test` 和获批 GUI `npm run verify` 均退出码 0。阶段 E 证据门禁已完成，可报告 0.3.0 候选状态；正式发布仍需另行授权。

## 已证明

- `author-cross-entry-electron.js`：真实作者派生副本上的 Chat → Navigation → 正文 Diff → 冲突阻止 → 重试接受 → Safe Undo 1/1；源稿 Markdown 快照不变，fixture 无真实外部网络请求。
- `author-affected-electron.js`：获批 GUI 环境真实通过 1/1，覆盖 Chat、取消、硬超时、Chapter、Research、Graph、图片预览/废纸篓/插入、Diff、接受和 Changes Safe Undo；图片插入按 workspace persistence 合同验证允许写入边界，不创建 Changes History。
- 跨项目 A→B 迟到结果：Provider 进入真实 18 秒延迟边界后，生产 reload/open-recent 路径切换到 B；测试等待 Provider 完成 latch 后确认 A 响应未显示，A/B Markdown 快照均未变化；随后切回 A，原作者路径继续通过。
- 阶段 A–D 的局部专项与 focused Electron 证据已记录在路线图和状态账本中；不能替代阶段 E 全量作者验收。

## 独立复审结论

- 独立复审已读取当前源码、测试、文档和首红证据，并明确 P0/P1/P2。

## 本轮独立复核判定

- P0=0：未发现会破坏数据安全、项目隔离、作者确认写入边界或任务权威性的阻断问题。
- P1=0：未发现会阻止真实作者完成核心任务流的高优先级问题。
- P2=3：大型 Electron harness 非固定时序红灯；workspace quick-open 单次 4,968.5ms > 1,500ms、定向重跑通过；作者目录私有防嵌套标记导致直接重复 copy 的安全预检首红（安全行为，非产品失败）。
- 图片插入正文后的允许写入范围已通过；图片不创建 Changes History，Safe Undo 由 Chapter/Research/Graph Changes 路径证明。
- `author-cross-entry` 与 `author-affected` 均在获批 GUI 环境通过；沙箱首轮 `listen EPERM 127.0.0.1` 保留为环境红灯，未计入产品失败。
- `author-cross-entry` 更新后的 Chat/Navigation manifest 对照曾因错误假设字段形状和字节语义各出现一次首红；均校准到生产合同后通过，未修改产品权限或校验。
- Research manifest 首次真实作者验收已通过；界面只显示 revision、来源数量和预算元数据，不显示来源正文或绝对路径。
- 来源充足/不足显示条件双分支真实作者验收已通过：充足分支不显示“添加来源”；不足分支严格返回 `needs_sources`，显示“添加来源”恢复动作，点击后选择来源可恢复原 action identity、回到 `review` 并保持 Diff 零写入；该 fixture 通过结构化 finding 标记绑定，不解析自由文本。
- 当前 worktree 的 `npm test` 首轮曾因 Sources race timer 未清理而滞留，已修复 `sources-view.js` 的 timer owner/清理边界；Research Renderer 改动后的完整 `npm test` 退出码 0、完整 `npm run verify` 退出码 0（获批非沙箱 GUI 环境，2026-08-04）。
- Graph、图片入口级取消/超时作者矩阵（Chat、Navigation、Chapter、Research 已有真实作者取消；Chat 另有 60 秒硬超时）。
- Chat、Navigation、Research、Chapter、Changes 的 `edit.md` Manifest 字段、预算、revision、遗漏/截断/不可用原因和 UTF-8 字节语义已由统一 v2 contract、服务专项及真实作者路径覆盖。
- 阶段 E 专门的 P0/P1/P2 终审签收。

## P2 / 首红记录

1. 大型 Electron harness 曾在不同位置出现非固定时序超时；一次 38/38 不作为稳定阶段 E 证据。
2. `verify-v0-workspace-location.js:308` 曾出现 300-file quick-open 4,968.5ms > 1,500ms；定向重跑通过，首红保留为环境/时序方差证据。
3. 作者选定源目录已有私有 `author-acceptance-copy.json`；直接重复 copy 的 `COPY_MANIFEST_CONFLICT` 首红证明防嵌套保护生效，验收副本通过临时 staging 后再由生产 transaction 创建。

## 下一步与签收规则

作者路径、两个作者 Electron 脚本、五入口 Manifest、五入口 revision 漂移、来源双分支恢复、跨项目迟到结果隔离、入口取消/Chat 硬超时和完整回归均已保留证据。P0=0、P1=0，P2=3 已明确登记；路线图 E 的真实运行退出条件已满足。本记录作为阶段 E 独立复审签收记录，0.3.0 进入候选状态，正式发布仍需另行授权。
