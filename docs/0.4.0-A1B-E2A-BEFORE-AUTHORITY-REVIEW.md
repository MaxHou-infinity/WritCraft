# 0.4.0 A1b / E-2a Before-Authority Review Boundary

> Date: 2026-08-13
> Scope: Stage A / A1b / E-2a before-authority only
> Status: payload content review is P0=0, P1=0, P2=0; final hash/diff binding review is pending

## 1. Signed claim and explicit non-claims

This checkpoint signs only the production E descriptor authority that must be
proven before any public Markdown or private recovery-record mutation. It
binds present/absent raw base History, the held `.writcraft` parent identity,
marker, artifact, project/recovery chain, public ancestors, and the current
leaf. The native helper repeats the complete authority validation after the
test pause and before it may return its current fail-closed result.

It does **not** sign production E mutation, `COMMITTED` or `UNCOMMITTED`, R/V/F,
`ROLLBACK_CREATE`, mixed transaction wiring, A1b, Stage A, A1c, A2, A-to-B, a
0.4.0 candidate, or release authorization. The valid production E fixture
still exits red with actual `UNKNOWN` and expected `COMMITTED`; only E-2b
single-leaf mutation is unlocked after this binding is signed.

## 2. Frozen payload manifest

The review record itself is outside the payload to avoid recursive hashing.
The same independent reviewer must bind this record's final hash.

```text
93371c269ad9dcde4b91daa50f8b4d68a8f3ff036483e28fd134b603ed7c84ba  docs/0.4.0-A1B-EXISTING-STATE-MATRIX.md
ad63ca0ab4be3ff2add73c8303ef1ac87184626482e23c72813e3e93e6a9f498  docs/0.4.0-A1B-E1-PARITY-REVIEW.md
62cdce56c72416e20f3b20bc9ca0b8d71f9c646dc0c96d1520116b7e5c2b2763  docs/CHANGES-HISTORY-RECOVERY-V1-CONTRACT.md
10eebdfd15084ed6f92cee305ddee5c85794dd709ab592c8b0009f1c0963d579  v0/DEVELOPMENT-STATUS.md
5e8fdbe5e196460758d9fcae4d06104e8a88e93142996bb239ed352aa7f11403  v0/src/main/snapshot-existing-restore-native-schema.js
c83db9127a419640eadc887427e248359c9440a9c915ad3b0c91778dfd683856  v0/native/public-markdown-create-helper.c
6c0d6cb568bfa07e04d95cc419c7d33fc1fdd985e8aaa88a17729b9ce1f66dce  v0/tests/verify-v0-snapshot-existing-restore-native-schema.js
6d73d23f5bc28a0cd2acf596813f40d484772d2820c5911cf315f8bcc4ab041e  v0/tests/verify-v0-public-markdown-native-lifecycle.js
```

The complete concatenated binary diff binding is:

```text
sha256:a1fb9d6715eb40994f759f17c155d85308aff2cc673e4ece5193a76dbf5245eb
```

It is reproduced in this exact order by the tracked binary diff against HEAD
for `docs/CHANGES-HISTORY-RECOVERY-V1-CONTRACT.md` and
`v0/DEVELOPMENT-STATUS.md`, followed by `git diff --no-index --binary
/dev/null <file>` for the six untracked payload files in manifest order.

## 3. Preserved red and focused evidence

The production first-red remains `UNKNOWN != COMMITTED`. The independently
reviewed E-2a evidence is:

- schema `20/20`;
- four production baselines: present History, absent History, writable held
  History fd, and unsafe History-parent mode;
- nine real paused-helper drift cases: History same/new inode, absent-History
  foreign creation, leaf same/new inode, marker same/new inode, recovery
  directory replacement, and nested public-ancestor replacement;
- canonical-only lifecycle `66/66`;
- strict Clang builds for normal, canonical, and paused helpers;
- registration `219` (`50` current, `169` legacy; `38` Stage A current);
- `git diff --check` with no output.

The independent reviewer froze the seven implementation/contract/test files,
reran these boundaries, rechecked every hash, and returned P0=0, P1=0, P2=0.
The earlier E-2a NO-GO and the review window invalidated by a subsequent schema
edit remain historical first findings; they are not overwritten by this green
batch.

## 4. Dispatch boundary

After final binding review, only E-2b single-leaf production mutation may
start. R/V/F, multi-item completion, `ROLLBACK_CREATE`, mixed transaction,
A1c, A2, Stage B revalidation, C/D/E, candidate, and release remain frozen.
