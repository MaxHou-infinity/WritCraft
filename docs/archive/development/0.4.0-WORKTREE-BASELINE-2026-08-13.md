# WritCraft 0.4.0 治理重置前工作树基线

> 日期：2026-08-13
> 用途：只读恢复与审计证据；不拥有当前派工权，不代表 A1b、Stage A 或 0.4.0 签收。

## 恢复点

- 基准 HEAD：`cb56c97` (`docs(roadmap): freeze 0.4.0 stage 0 contract`)
- 治理重置前 WIP 恢复提交：`3fa85863577fd661903bb942e61ebe360a49bece`
- 仅本地引用：`refs/wip/governance-reset-2026-08-13`
- 该引用不属于产品分支，不得 push、merge 或当作 checkpoint 签收。

恢复检查可以在新的临时 worktree 中进行：

```sh
git worktree add --detach <temporary-path> refs/wip/governance-reset-2026-08-13
```

## 重置前事实

- `git status --short` 有 138 个状态项：31 modified、1 deleted、106 untracked。
- Git 在恢复提交中将 `renderer/marked.umd.js` 与 `shared/marked.umd.js` 识别为一次 rename，因此提交统计为 137 个 changed files。
- tracked diff 约为 `+9,141/-355`；未跟踪文件约 4.54 MiB。
- Stage A、A1a/A1b、已冻结 Stage B、Renderer、package 和治理文档混在同一工作树，不是一个可独立评审的 A1b change set。

## 机械分类

| 类别 | 重置前状态项 | 处理原则 |
|---|---:|---|
| 治理、路线图、状态与 review 记录 | 10 | 本轮只精简活跃权威，历史证据归档；旧状态逐字快照保留为 `v0/DEVELOPMENT-STATUS-THROUGH-2026-08-13.md` 以维持原相对链接 |
| A1b / Changes-History 共享 authority | 57 | 保留 WIP，治理重置后再按单一 A1b checkpoint 完成 |
| 其他 Stage A Snapshot 能力 | 24 | 不与 A1b review 绑定，继续冻结 |
| 已冻结 Stage B delivery 能力 | 24 | 保留证据，等 Stage A 后只做 A→B 重签 |
| package / native binaries | 10 | 不参与 A1b 内部 slice review |
| 跨层或疑似无关改动 | 13 | 在进入候选前单独分类验证 |

## 治理约束

1. 不再对上述累计 WIP 做 per-file hash 和 full-binary-diff 微切片签收。
2. 后续独立 review 必须绑定单一 clean checkpoint commit/tree。
3. A1b 内部 schema、wire、native bridge 只是 component evidence；A1b 出口只进行一次独立复审。
4. 恢复点仅用于找回工作，不能覆盖治理重置后的当前状态。
