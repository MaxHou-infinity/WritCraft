# 笔触 · WritCraft 当前开发状态

> 最后更新：2026-08-13（A1b primary one-leaf E final binding 已 P0=0、P1=0、P2=0；E-3 permanent-journal publication 与 EXISTING journal-binding pure schema 修订复核亦为 P0=0、P1=0、P2=0。EXISTING journal binding focused、existing native schema 23/23、registration 220、Node syntax 与 diff-check 已绿。E-3 journal-binding review final binding 仍待同一 reviewer 签字；签字前不得开始 Main CAS/native fresh R。V/F、`ROLLBACK_CREATE`、mixed transaction、A1c 与 A2 仍冻结，A1b 总体仍 NO-GO，代码版本仍为 0.3.0。）
> 当前公开版本：`writ-craft@0.3.0`，npm `preview`
> 当前代码版本：`v0/package.json` 为 `0.3.0`
> 当前发布版本：`0.3.0` 透明 AI 协作（npm `preview:0.3.0`，GitHub `v0.3.0` prerelease，`latest:0.1.0`）
> 下一目标版本：`0.4.0` 证据与交付闭环（`WRC-0.4.0-R1` 已批准）
> 当前阶段：**Stage A 封版恢复；A0、A1a、A1b primary one-leaf E 已签收；E-3 publication 与 journal-binding pure schema 内容复核已绿、final binding 待签，A1b 总体仍 NO-GO。binding 签字前不得开始 Main CAS/native fresh R；下一窄层仅为 native/C journal bridge，V/F、`ROLLBACK_CREATE`、mixed transaction、A1c、A2 与 Stage B/C/D/E 继续冻结；当前代码版本仍为 0.3.0**

本文件只记录当前事实、开放风险和下一动作。0.1.x 的完整里程碑、红灯、测试数字和验收过程已归档到 [`docs/archive/development/DEVELOPMENT-STATUS-THROUGH-0.1.2.md`](../docs/archive/development/DEVELOPMENT-STATUS-THROUGH-0.1.2.md)，不得从归档旧 TODO 直接派发工作。

## 0. 2026-08-11 当前控制块

- **执行协议**：[`docs/0.4.0-EXECUTION-PROTOCOL.md`](../docs/0.4.0-EXECUTION-PROTOCOL.md)（`WRC-0.4.0-EXEC-R1`）。版本目标仍是 `WRC-0.4.0-R1`，但不再使用无界目标循环；当前采用单 checkpoint、受约束里程碑恢复。
- **A0 签收**：0.4 测试清册、静态 orphan check 与 current-component 顶级 npm gate 已落盘并实跑；所有 Stage A component 证据固定 `completionEligible=false`。独立复核 P0=0、P1=0、P2=0；exact payload/hash/diff/reviewer 边界见 [`docs/0.4.0-A0-EXECUTION-GATE-REVIEW.md`](../docs/0.4.0-A0-EXECUTION-GATE-REVIEW.md)。
- **A1a 签收（2026-08-11）**：CREATE finalize/ACK cleanup 已完成真实 Main/native permanent-journal 纵切并独立复审 P0=0、P1=0、P2=0。15-file frozen payload 已绑定 hash；证据覆盖 response-loss/restart、ACK_PREPARED/ACK_COMMITTED、terminal clear 两个真实漂移窗口，且明确不宣称已提交槽回滚或 journal CAS 原子共验。focused evidence：public schema 14/14、marker schema 23/23、artifact lifecycle 19/19、native CREATE lifecycle 28/28、public lifecycle 63/63、real finalize 1/1、transaction phase 85/85、ordinary journal 52/52、production wiring 1/1、registration 219（50 current/169 legacy）、strict Clang、Node syntax、`git diff --check` 全绿。此签收仅覆盖 A1a，不外推 Stage A、A→B 或 0.4 candidate。
- **当前唯一主任务 A1b**：只收口 mixed EXISTING/`ROLLBACK_CREATE`。不得同时开始 Safe Undo、A2 App 接线或 Stage B/C/D/E。
- **A1b primary E 签收（2026-08-13）**：one-leaf production E 的 exact manifest/hash/diff final binding 已由同一 reviewer 复核 P0=0、P1=0、P2=0；记录见 [`docs/0.4.0-A1B-E2B-PRIMARY-E-REVIEW.md`](../docs/0.4.0-A1B-E2B-PRIMARY-E-REVIEW.md)。只解锁 E-3，禁止重放 E；不签 multi-item、R/V/F、`ROLLBACK_CREATE`、mixed transaction、A1b 总体或 Stage A。
- **A1b 当前 checkpoint（2026-08-13，E-3 journal-binding schema final binding 待签）**：永久 journal VALUE 的 `existingTerminalPublication` 与 WRCCHRJ2 `EXISTING_JOURNAL_BINDING` pure schema 均已形成；binding 冻结完整 journal/root/recovery identity、slot/head/predecessor、VALUE payload/active-marker slices、dual digest、E/R 23-field header 与 exact max/+1 budget。首轮 binding review P1=2（journal budget 未复用 exact constants、header 缺 operationId）；修订后同一 reviewer P0=0、P1=0、P2=0。记录见 [`docs/0.4.0-A1B-E3-JOURNAL-BINDING-SCHEMA-REVIEW.md`](../docs/0.4.0-A1B-E3-JOURNAL-BINDING-SCHEMA-REVIEW.md)，final binding 前不得开始 Main CAS/native fresh R。
- **A1b review-drift 事实（保留）**：首轮独立复核期间 C helper 发生 reviewed-file hash 漂移，该轮按协议作废；随后以新六文件 hash 重新冻结复核，未再漂移。旧 findings 不冒充新签收。
- **后续顺序**：A1a finalize/ACK → A1b mixed/`ROLLBACK_CREATE` → A1c Safe Undo → A2a create/list → A2b compare → A2c restore/Safe Undo → A2d delete → A3 helper package/Stage A 门禁/真实 Electron/Computer Use/独立复审/exact Stage A checkpoint → 使用 App 创建的真实 snapshot 重签 A→B。每个后缀独立首红、退出证据、复审和冻结边界；此前禁止 C/D/E。
- **当前硬阻断**：Stage A 的 A1b mixed/rollback、A1c Safe Undo、A2 App/事务与 Stage A 门禁仍未闭合；Stage C DOCX 未实现；Stage D 三个新视图和连续交付未实现；Stage E 候选/作者/Pages/复审未开始；`v0/package.json` 仍为 0.3.0。
- **冻结纪律**：当前大量 Stage A/B 修改尚未形成 implementation candidate；任何下一纵切前必须先完成当前 checkpoint 的独立签收与 exact local checkpoint commit。若不能提交，必须暂停并形成 exact file manifest、逐文件 hash、完整 diff 绑定与 reviewer 签字，或采用所有者明确批准的隔离边界；文字原因不能解锁下一纵切。
- **测试纪律**：当前 `npm test` 与 Stage B/Stage A focused 绿灯只证明各自范围。A0 必须先使全部现存 0.4 脚本进入显式清册/注册检查；此后新增或改名 `verify-v0-*.js` 必须在同一 change set 注册。未注册测试、seeded snapshot、fake adapter 或 focused 绿灯不能签收 App/Stage。
- **资源止损**：一个主 checkpoint + 最多一个不改同一 authority 的支持任务；同类 P1 连续两轮即回到完整矩阵；定向门禁未绿前不重复完整回归；review 期间冻结文件。
- **流程首红（保留）**：2026-08-11 首轮治理 review 开始后，执行协议仍有两处措辞漂移，审查员据 hash 变化判定 freeze 失效。本轮不得被最终绿色覆盖；以后 reviewer 开始时记录全部被审文件 hash，任一漂移使整轮无效并从新 hash 重启。
- **A0 可执行证据（2026-08-11）**：当前 hash 的 `npm run verify:0.4:registration` 退出码 0，对全目录 217 个 `verify-v0-*.js` 完成无遗漏分类：48 个 0.4 current、169 个显式 legacy baseline；current 中含 36 个 Stage A component、9 个 Stage B Node preflight、1 个 GUI-only Electron。36+9 个 component/preflight 在本轮编排纠偏前已顺序实跑一次并全部通过；后续只修改 registration/orchestrator，未改这些 component 或产品代码，因此按资源止损规则不重复运行。`pretest`/`preverify` 只执行廉价静态注册检查，45 个 Node component 只由显式 `verify:0.4:current-components` 聚合一次。以上只证明清册完整与当前组件回归，不证明 Stage A、A→B 或 0.4 candidate；GUI-only Electron 未运行。
- **最新 A1a 复验（2026-08-11）**：A1a frozen payload 的定向证据与独立复审均已完成；未运行 GUI/full suite，因此不得把 A1a focused sign-off 外推为 Stage A、A→B 或 0.4 candidate。
- **下一动作**：先完成 E-3 journal-binding review record 的 final binding。签字后只启动 native/C journal bridge，再启动 post-E Main journal CAS + native fresh read-only R：raw E response 在 CAS 前丢失仍为 UNKNOWN/manual；只有 durable `existingTerminalPublication` 才允许 fresh R descriptor-reopen stored identities，禁止重放 E 或从当前 records 重铸 identity。V、F、`ROLLBACK_CREATE`、mixed transaction、A1c 或 A2 继续冻结。

## 1. 权威入口

1. [`docs/ROADMAP.md`](../docs/ROADMAP.md)：当前版本顺序、0.4.0 范围和非目标。
2. [`docs/ROADMAP-0.4.0.md`](../docs/ROADMAP-0.4.0.md)、[`docs/EVIDENCE-DELIVERY-V1-CONTRACT.md`](../docs/EVIDENCE-DELIVERY-V1-CONTRACT.md)、[`docs/0.4.0-EXECUTION-PROTOCOL.md`](../docs/0.4.0-EXECUTION-PROTOCOL.md) 与 [`docs/0.4.0-STAGE-0-INDEPENDENT-REVIEW.md`](../docs/0.4.0-STAGE-0-INDEPENDENT-REVIEW.md)：0.4.0 生效范围、冻结合同、当前执行控制和阶段 0 签收；当前只可派发 Stage A/A1b 工作。
3. [`docs/ROADMAP-0.3.0.md`](../docs/ROADMAP-0.3.0.md)：已完成并冻结的 0.3.0 合同。
4. 本文件：当前代码、开放风险和下一动作。
5. [`docs/WRITCRAFT-PRD-V3.md`](../docs/WRITCRAFT-PRD-V3.md)：长期产品契约。
6. [`docs/ARCHITECTURE.md`](../docs/ARCHITECTURE.md)：稳定架构与维护边界。
7. [`docs/INDEX.md`](../docs/INDEX.md)：按任务选择需要阅读的合同，禁止全量读取历史文档派工。

源码与可复现测试优先于文字快照。任何文档冲突先校准文档，再继续开发。

## 3.1 2026-08-10 Stage A 后端基线冻结

- 本次收口只冻结 Stage A 的后端永久 journal 基线：journal schema/physical writer/native wire 与 DISCOVER、ordinary journal read/write/terminal-IDLE、full-missing Snapshot 的 PREPARE→CREATE_MISSING capture→`CREATED_RECEIPT`→History durable→`HISTORY_COMMITTED`。它证明的是 Main/service 可继续接线的可重建权威，不是完整 App 功能完成。
- 最终定向证据：`npm test` 通过；Stage A focused 通过：journal schema 23/23、native wire 11/11、physical journal 12/12、native lifecycle 10/10、ordinary journal write 52/52、journal read 8/8、phase 15/15、transaction phase 84/84、restore recovery 13/13、restore service 39/39、public native schema 14/14、public native lifecycle 63/63、CREATE_MISSING native/lifecycle 26/26、Safe Undo schema/lifecycle 10/10 与 26/26、artifact lifecycle 19/19；相关 Node syntax 与 `git diff --check` 通过。完整 `npm run verify` 已在与产品相同的非沙箱 GUI 环境通过，沙箱内的 Electron SIGABRT 仅作为环境证据保留。
- 本基线明确不签收：CREATE finalize/ACK cleanup、Safe Undo D/FINALIZED/ACK 的当前接线、`ROLLBACK_CREATE` Q/R 的跨进程 publication identity、mixed EXISTING、Snapshot helper package，以及作者验收、发布或推送。Main/IPC/Renderer 与真实 Electron 的 Stage B 交付预检已在下方 3.2 独立签收；本段仍只约束 Stage A 后端基线，纯 finalization/settlement 文件不得把 focused 绿灯当成 Stage A 集成完成。
- 工作区中的历史 focused 证据和未接线窄层仍保留，但以上边界优先级最高；下一次开发只能从本节列出的未完成项重新立项，不能从旧 TODO 或旧“已签收”句子派工。

## 3.2 2026-08-10 Stage B App 交付预检最终验收

> **Stage B App 最终证据（2026-08-10）**：真实 Electron 隔离项目旅程 1/1；已提交 snapshot、Markdown 字节零写入、无 HTTP(S) 请求、Renderer 不暴露 root。native image worker 8/8（含 40 次调用、40Mpx 最大合法 fixture、verified bundle fd + snapshot-entry-binding、CRC 合法但损坏 PNG/JPEG hostile）已通过。原有“尚无真实 Electron”与“bundle-fd/entry-binding 未签收”句子仅为历史 checkpoint 证据，不再代表当前状态。

> **打包回归（2026-08-10）**：`npm test` 与批准环境 `npm run verify` 退出码 0；strict Clang、Node syntax、`git diff --check` 全绿，新增 ImageIO helper 已纳入 universal build attestation、Contents/Helpers 签名与 ZIP 复核。Stage B 两个图片合同阻断已关闭；DOCX 属 Stage C，真实作者属 Stage E。

- 本检查点只覆盖 Main 离线纯层：`delivery-preflight-schema.js` 冻结 delivery authority、Graph/SourceIndex cross-binding、五类 citation health、manifest/public projection、warning/blocker/capability gate、owner generation 与 4 MiB public envelope；`delivery-preflight-service.js` 只消费 immutable snapshot bundle、Main-owned Graph/SourceIndex，不读取实时正文、不写正文、不发网络请求。
- 定向证据：delivery service **22/22**、delivery schema **6/6**、evidence-delivery schema **22/22**、SourceIndex **10/10**、shared marked **4/4**；三份 Stage B JavaScript 与现有 source-index-service `node --check` 通过，`git diff --check` 通过。证据覆盖真实 Graph block anchor 正向 fixture、嵌套 blockquote fenced-code masking、codespan/frontmatter masking、定义行不计引用、跨文件 footnote duplicate、非法 key fail-closed、source marker/source-offset binding、inline HTML/image-alt masking、同一图片多引用逐一 token 绑定、PNG CRC/IDAT/IEND、JPEG single-frame/EOI、native decoder failure gate、SourceIndex locator/content hash mismatch、definition-only footnote、health-root digest、public evidence locator 与预算边界。
- **Main handler boundary 已签收**：`delivery-preflight-handler.js` 以依赖注入方式冻结 `trusted sender → exact request/binding → settleAuthority watcher barrier → Main-owned readers → 前后漂移复核 → service`；内部错误只映射合同稳定 code，完成后若 owner binding 漂移会回收已签发 capability。focused **7/7**：新增真实 owner deadline/abort（测试用 1 秒缩短窗口验证 120 秒默认预算），并覆盖 barrier 失败零读取、Renderer authority 字段拒绝、snapshot unavailable fail-closed、读取期间 drift 零 capability、issue 后 drift 回收 capability。该 handler 负责 preflight deadline 与 cooperative abort；更高层 local-task UI/取消状态仍需后续 Main orchestration。
- **Main/IPC/Renderer wiring 已签收**：`main.js` 已注册 `writcraft:project:delivery-preflight` 与 snapshot list/files 只读 IPC，preload 暴露窄桥，Sources 面板可选择已提交 snapshot/Markdown 并显示 public preflight；Main adapter 使用 committed snapshot worker、snapshot-bound Graph/SourceIndex provider 与 request-scoped capability store。Renderer 只发送 `projectInstanceId/snapshotId/orderedFiles/warningDecision`，不发送 root/content/Graph/SourceIndex。证据：capability store 7/7、provider 9/9、Main adapter 3/3、handler 7/7、IPC boundary 4/4，批准环境真实 Electron 1/1，`npm test` 与 `npm run verify` 通过。
- **Stage B App 已完成最终验收**：`delivery-preflight-service` 22/22、schema 6/6、evidence 22/22、provider 9/9、handler 7/7、capability 7/7、IPC 4/4、ImageIO worker 8/8；批准环境真实 Electron 1/1；`npm test` 与 `npm run verify` 通过。生产 Main 使用独立 ImageIO/CoreGraphics helper，接收 verified bundle fd + snapshot-entry-binding，native 复核 bundle/entry identity、footer、offset/length、digest 与 post-decode identity；PNG IDAT/scanline 与 JPEG baseline entropy/EOI hostile 均 fail-closed。DOCX build/consume 属 Stage C，作者隔离 zero-write 属 Stage E；provider 对缺失或 Graph identity 不匹配的 snapshot-bound correction artifact 直接 stale/block，不会静默用 fresh graph 铸造 capability。
- 防回滚约束：Stage B 纯层作为新增、只读、无写入的独立检查点保留；后续 IPC/Renderer 只能消费其 public projection 和 opaque capability，不得把 Renderer 输入、实时路径或旧正则结果提升为 authority，不得删除本节测试或用单元绿灯替代真实 Electron/作者验收。

## 2. 0.1.2 已发布基线

- npm：`writ-craft@0.1.2`，`preview: 0.1.2`，`latest: 0.1.0`。
- registry shasum：`553bec35246b118ae5a47b2d4dd327c16c171029`。
- GitHub prerelease：`https://github.com/MaxHou-infinity/WritCraft/releases/tag/v0.1.2`。
- annotated tag `v0.1.2` 解引用到发布提交 `395b863`；文档收口提交为 `95f923b`。
- 不提供未签名 App/ZIP；独立 macOS App 签名与公证不是当前路线。
- 0.1.2 已冻结。后续生产修复使用新版本，不回写或重新发布 0.1.2。

已签收的用户能力：

- 本地多文件项目、`edit.md` 项目 Prompt、文件树、标签和恢复；
- 项目卡、Inline `⌘K`、Chapter、Chat 与 Context Inspector；
- Changes/Diff、History、冲突阻止和 Safe Undo；
- 空项目结构规划、已有稿件写作导航和统一写作任务流；
- Sources、Research 高级独立流程、Graph v2 与 Issue→Changes；
- 图片生成、评分、prompt-free 插入、保留和安全删除终态；
- Markdown 回收区、诊断预览、npm Developer Preview。

以上能力除非 0.2.0 生产源码直接影响，不重复进行 0.1.2 的项目卡、接受/拒绝/撤销或付费作者验收。

## 3. 0.3.0 已完成执行事实

- 阶段 0：当前源码核对已完成。旧 Project Plan 用户运行时已在 0.2.0 阶段 0 物理移除；现存 `chapter-generation-plan/v1` 仅属于 Chapter 内部区块规划，不得删除或误归类为退役 Project Plan。
- 阶段 0：现有 `context-selection`、Main `context-resolver`、Context Inspector、`unified-writing-task-service` 和 Navigation 的取消/超时机制均已确认，0.3.0 只补统一入口和跨模块状态，不重复造解析器或 Key 存储。
- 阶段 A：新增 Main-only `ai-task-state-service.js`，统一 task/attempt/owner/project/revision、15 秒取消、60 秒硬超时、迟到结果和项目切换失效；Navigation 动作已接入，Chat/Chapter/Research/Graph/Changes/图片入口已通过 `runAiRequest` 声明任务 kind/target。
- 阶段 B：新增 Main-only `context-catalog-service.js` 与窄 IPC；文件、文件夹、章节、段落候选已从真实树和 revision 快照生成，来源/实体仅在明确搜索前缀时有界索引。Renderer 新增不重建输入节点的 `@` 补全；候选由 Main 签发短期 `@ref:<catalog>:<candidate>` 引用，提交时必须通过当前 project/revision 校验，过期或未知引用 fail-closed。Chat、导航目标和 Changes 指令已接入；候选不返回正文、root 或 capability。真实项目 focused 验证 1/1 与 Context Catalog real Electron 1/1 均通过，确认来源 revision、实体 ID、章节 revision、有效引用进入统一项目上下文，edit.md revision 漂移、foreign project identity 和 TTL 过期会失效；Chat UI 显示“上下文候选已过期，请重新选择”。目录同时覆盖 `person/place/organization/variable/concept/event/datum` 等图谱节点，并按 `entity/source` 前缀解释查询。真实作者五入口现均输出同一 Main-owned Context Manifest v2 envelope。
- 阶段 C：Chat 既有 Context Resolver 继续作为权威入口；Navigation、Chapter、普通 Changes 和 Research 已接入同一 `compileEditPrompt` 有界编译结果。Research 的来源卡片仍由用户显式选择，但模型同时收到 Main 编译的 edit.md 项目 Prompt，并在 Context Manifest 中披露 edit.md revision/预算；不改变独立 Research 的来源与写入边界。
- 阶段 D：Renderer 已挂载统一任务进度视图，Main 的 Chat/导航/Chapter/Research/Graph/Changes/图片入口声明真实业务阶段；任务服务严格要求 `attemptId` 字段（未提供时由 Main 生成），并在成功、失败、取消、超时和项目切换后清理 owner。项目卡真实 Electron 4/4 通过，覆盖冷启动、长正文滚动、严格 onboarding 审阅和初始文件二阶段确认。
- 阶段 D：同一 profile 的 API Key “保存 → 完全退出 → 重启 → 状态恢复 → 发起请求”真实 Electron 专项已通过；设置页密码框仍按安全设计为空，状态行是配置真相。此前的 P1 记录保留为历史作者反馈，不再作为当前未决缺陷。
- 本轮真实红灯已分类并修复：快速失败导致进度提示一闪而过、项目尚未 ready 时项目卡提前打开、严格 task 合同缺少 `attemptId` 键、成功提案后复用旧 review 草稿，以及 E2E 夹具未跟随 readiness/阶段数合同更新。修复后 Onboarding focused Electron 为 4/4，相关动态 Renderer 为 30/30，workspace draft owner 为 4/4。
- 阶段 A/D 最新真实作者路径（2026-08-04）：在同一稳定 profile、已配置 `sk-cp-` Key 的隔离副本中，Navigation 生成耗时约 9.1 秒并生成建议；点击“处理这个建议”后约 15 秒内在正文编辑区显示 1 项 Diff，界面明确“尚未写入；接受后才会修改文件”。退出审阅后明确保持零写入。随后真实点击“停止整理”可在约 1 秒内终止请求，正文不变；取消结果已收口为 `REQUEST_ABORTED`/`cancelled`，不再伪装成通用失败。
- 本次真实路径还定位并修复一个 P1 根因：Navigation 把含绝对路径和 JSON 标点的内部 lease key 误作 AI task owner，导致 provider 尚未调用即 28–37ms 失败。现改为 `navigation_<attemptId>` 有界 owner，并为返回式 `REQUEST_ABORTED` 保留 cancelled 终态；Main wiring 8/8、Navigation service 专项通过，修复后的真实生成与 Diff 已验证。
- 2026-08-04 中间回归记录：一次 `npm test` 通过，获批 GUI 环境一次 `npm run verify` 退出码 0；本轮新增写作导航服务 31/31、AI task 7/7、进度 Renderer 3/3、Main wiring 8/8、Context catalog 2/2、真实项目 Context catalog 1/1、Context Catalog real Electron 1/1（含短 TTL 过期和 Chat 恢复 UI）、Context autocomplete 2/2，API Key 同 profile 重启 1/1、watcher Main/IPC 3/3、Chat 任务进度 focused Electron 1/1。统一写作任务 focused Electron 1/1 另行验证一次主要点击到正文 Diff、拒绝/接受/Safe Undo；随后阶段 E 已补齐跨入口作者验收、独立复审与最终完整回归，不能再把这条中间记录当作当前未完成状态。
- 阶段 E 新增真实作者跨入口专项 1/1：从所有者指定的 `WRITCRAFT_E2E_AUTHOR_PROJECT` 源稿经生产 author-acceptance copy transaction 创建 disposable derived copy，仅在副本新增 `chapters/author-e2e.md` fixture；真实 Electron 完成 Chat（项目 Prompt + 当前文件 Context Chips）→ Navigation（一次主要动作）→ 正文 Diff 零写入→外部改动后的冲突阻止→退出审阅重试→接受写入→History Safe Undo。专项还证明源稿 Markdown 快照前后一致、AI fixture 没有真实 HTTP(S) 请求；首轮 Chat 上下文断言过窄和二次审阅状态等待红灯均已保留并校准后通过。
- 2026-08-04 统一任务 focused Electron 1/1：在隔离临时项目和本地 AI fixture 中验证单一“处理这个建议”主动作、正文内 Diff、预览零写入、拒绝零写入、接受后写入、History 和 Safe Undo；未发送真实稿件或 Key。该专项不能替代 Chat/Chapter/Research/Graph/图片的跨入口作者验收。
- 阶段 E 作者验收（2026-08-05）：所有者提供的作者目录包含私有 `.writcraft/author-acceptance-copy.json`，直接重复 copy 首轮以 `COPY_MANIFEST_CONFLICT` 被安全预检拒绝；未删除或改写源目录，测试仅在临时 staging 排除该私有防嵌套标记后再由生产 copy transaction 创建副本。获批 GUI 环境 `e2e:electron:author-cross-entry` 1/1 与 `e2e:electron:author-affected` 1/1 均通过：Chat/Navigation/Chapter/Research/普通 Changes 的 Provider 边界 edit.md revision 漂移均丢弃旧结果，Markdown 零写入；并覆盖 Chat 正常/取消/60 秒硬超时、Chapter、Research、Graph、图片预览/评分/废纸篓/插入正文、正文 Diff、冲突阻止、接受和 Safe Undo；来源不足“添加来源”恢复、跨项目 A→B 迟到结果丢弃及 A/B 零写入继续通过。五入口 Context Manifest v2 对照和相对路径/正文不泄露专项通过，fixture 无真实外部请求，源目录 Markdown digest 前后一致。
- 本轮保留的首个回归红灯是 `verify-v0-sources-race.js` 测试夹具未提供 Renderer 新增的 `window.setTimeout`；该夹具已补齐 `setTimeout/clearTimeout`，定向 Sources race 5/5 通过。修复后当前 worktree 的完整 `npm test` 退出码为 0。
- 本轮 `npm run verify` 首个红灯为 `verify-v0-workspace-location.js:308` 的 300-file 内存 quick-open 性能断言（实测 4,968.5ms > 1,500ms）；未修改产品代码迎合单次测量，保留为当前验证环境/性能方差证据。随后仅重跑该定向用例通过，证明当前红灯具有非固定时序/环境方差，不能用该绿灯覆盖首红。
- 获批环境真实作者 `author-affected` 首轮红灯发生在图片插入后的 Safe Undo：插入已成功，但该 workspace persistence 路径不创建 Changes History 记录，因而没有图片专属 latest undo 卡；首红保留并已将断言校准到真实合同（图片只验证允许写入边界，Chapter/Research/Graph 验证 Safe Undo），不能把图片插入误报为有 History Undo。
- 当前 `npm test` 首轮未退出的首红已定位并修复：`sources-view.js` 的 Research 来源打开观察与 15 秒 fallback timer 在 Promise 结束后未清理，导致 Sources race 虽 5/5 通过但进程滞留；现已绑定 owner、清理 observation/timeout timer，定向 Sources race 5/5 约 0.06s 退出，修复后完整 `npm test` 退出码为 0。Research manifest 改动后的完整 `npm test` 与获批 GUI `npm run verify` 也均以退出码 0 完成。

## 4. 当前开放项

### 0.4.0 阶段 0 合同/基线冻结（已完成；下列旧段为历史基线）

- `WRC-0.4.0-R1` 已获所有者确认，§11 目标模式文本已另行提交；阶段 0 已完成。本文本块保留阶段 0 当时的派工快照；当前执行状态以 3.1 Stage A 基线冻结与 3.2 Stage B App 最终验收为准，不得再解读为“B–E 尚未开始”。
- 生效合同复用 History、Diagnostic Export、SourceIndex、Graph v2 和 Image Trash 的既有边界，但不把它们误报成项目快照、作品导出、多视图或健康审计。
- 已确认四项：DOCX 单格式；snapshot 不删除新增文件且只恢复 Markdown；Graph 四个共享证据视图；alt/caption 与已保留素材清理默认不纳入必做。
- 新增 `docs/EVIDENCE-DELIVERY-V1-CONTRACT.md`，冻结 Snapshot allowlist/容量/私有存储、创建和删除三态事务、比较与全有或全无的 Markdown 选择性恢复、八分量 delivery authority、五类引用健康、Citation shared 单一权威、DOCX 子集/OOXML/安全保存、Graph 四投影、本地 operation owner 和 Pages 14.4（7043.0.93）真实渲染基线。
- 阶段 0 首轮只读合同审计保留为 P0=0、P1=10、P2=3：P1 覆盖启动状态、Snapshot/schema/事务、恢复、delivery authority、DOCX、OOXML、健康/Citation、Graph 和本地长任务边界；已写回冻结合同，待新的独立复审确认。P2 保留当前文档 dirty worktree、Electron 沙箱 SIGABRT/获批环境版本探针差异，以及 0.3.0 大型 harness/quick-open 历史首红。
- 阶段 0 当前运行基线（产品源码、测试、脚本与 `v0/package.json` 零修改）：完整 `npm test` 退出码 0；沙箱内首次 `npm run verify` 在 `verify-v0-dom-sanitizer.js` 以 `Electron exited null` 红灯退出，原命令在获批 GUI 环境退出码 0，首红保留且不归因于产品代码。获批环境 `WRITCRAFT_E2E_FORCE=1 npm run e2e:electron` 连续两次在前 6 个阶段通过后，于“普通 Markdown 可见回收区列表”等待处同点红灯，并同时报告 watcher unavailable/`HOME_SNAPSHOT_TIMEOUT`；这是可复现的当前大型 harness 基线，不能宣称 38/38 全绿，也不在文档阶段修改产品。独立真实 Main/IPC watcher 专项 3/3 通过，证明确定性 watcher 边界可启动。
- Computer Use 已从 Pages 的真实 About 窗口读取 `Pages 14.4 (7043.0.93)`；WritCraft 使用单独临时 profile 启动并被系统注册为运行应用，但读取其 AX app state 连续返回 Computer Use server timeout `-10005`。该隔离进程与仅由本轮创建的临时 profile 已清理；自动化真实 Electron 证据与 Computer Use 工具超时分别记录，后者列为阶段 0 环境 P2，不能冒充 App UI 通过。
- 健康 marker 复用现有 Main `consistency-engine` 的 `【待补来源】`、`[待补来源]`、`[citation needed]`、`<!-- citation-needed -->` 四种语法；阶段 B 只把既有 `evidence_gap` 适配为 `missing_source`，不创建第二套扫描器或 marker parser。
- 阶段 0 最终稿首轮独立复审保留为 P0=0、P1=7、P2=3，明确阻止进入阶段 A。P1 为 `.markdown` 兼容、digest preimage、完整嵌套/事务/public schema、私有 stage/quarantine 原子所有权、五类 health binding/reason/severity、既有 Unicode citation/URL 兼容，以及 Markdown/图片 parser/预算权威；现已逐项写回合同，等待同一复审员定点复核。P2 为沙箱 DOM Electron 环境红灯、完整 Electron harness 连续同点红灯和 WritCraft Computer Use AX `-10005`；另把恶意同 UID 进程主动篡改 `0700` 私有 snapshot 存储明确列为 V1 主机账户攻陷残余，不再宣称由随机名或事后 stat 关闭。
- 第二轮定点复核保留为 P0=0、P1=5、P2=4：P1 指向 bundle/digest payload/数值上限仍非 exact、public Diff 与逐边界分页请求矛盾、Graph→Source binding 和 fragment fixture、真正图片 decoder、DOCX 直接创建 final/错误假设保存面板返回父 fd。现已冻结 bundle golden bytes 与字段上限、operation-specific request、同 graph identity 的 source binding table、ImageIO 强制 decode worker，以及 Main-only save path→可信 filesystem-root traversal→同目录 hidden stage→`RENAME_EXCL`→final reconciliation；待第三轮独立复核。P2 四项保持不变。
- 第三轮定点复核保留为 P0=0、P1=5、P2=4：剩余 P1 为 source/bundle object digest 双语义、Graph binding digest 未定域、public 标题标签与正文例外文案、list/delete/artifact capability 签发链、ImageIO 最大合法资源闭包和任意目标保存的持久化 binding/reconciliation。现已拆为 `sourceObjectIdentityDigest`/`bundleObjectDigest`，冻结 binding digest 域，明确 heading/label 为有界 metadata，补齐 list/delete preflight/artifact public envelope，收敛 DOCX 到 40 图并以双 worker/1 GiB/100 秒闭合，以及新增 private save target binding、stage/expected-final/published identities 与重启三态算法；待第四轮独立复核。
- 第四轮定点复核保留为 P0=0、P1=3、P2=4：P1 为 bundle/save identity 摘要仍缺 exact preimage、capability `selectionDigest` 未按 kind 冻结、ImageIO 误写成不存在的独立 snapshot object fd。现已补齐 bundle object framing、save volume/parent/stage/published identity exact payload，按 compare/restore/delete/export/artifact 冻结 selection payload，并将图片读取改为 verified bundle fd + exact entry offset/length/digest 的 `pread`；save recovery 的“只匹配 stage”也改为 cleanup + final 不存在重检 + 目录 fsync 后才发布 `UNCOMMITTED`。待第五轮独立复核。
- 第五轮定点复核保留为 P0=0、P1=2、P2=4：仅余 Graph source `bindingId` 是否等于完整 digest、entry digest 字段名笔误，以及 macOS `fsid_t` 有符号编码。现已明确 `bindingId =` 3.1.4 完整 `sha256:` digest、统一 `entryBindingDigest`，并把 `fsid0/fsid1` 冻结为 int32 原值的有符号十进制字符串。
- 第六轮定点复核最终为 **P0=0、P1=0、P2=4**，确认阶段 0 可以退出、阶段 A 可以开始。四项 P2 为：沙箱 DOM Electron 红灯/获批 GUI `verify` 绿灯的环境差异；完整 Electron harness 两次同点红灯；WritCraft Computer Use AX `-10005`；恶意同 UID 主动篡改私有 snapshot store 的主机账户攻陷残余。完整签收见 [`docs/0.4.0-STAGE-0-INDEPENDENT-REVIEW.md`](../docs/0.4.0-STAGE-0-INDEPENDENT-REVIEW.md)。
- 草案首轮独立复审保留首红：P0=0、P1=6、P2=4，指出 PRD 发布状态、D1→R1 顺序、图片恢复边界、snapshot 事务、统一 delivery authority、DOCX 真实渲染，以及 focused evidence、诊断导出隔离、citation 权威和 Graph 连续性缺口；均已写回草案。
- 第二位独立复审首轮为 P0=0、P1=1、P2=2，发现当前项目图片漂移与 exact-snapshot 导出语义冲突，以及 snapshot 删除事务和 `deliveryAuthority` 字段歧义；修订后由该复审员定点复核为 P0=0、P1=0、P2=0。该结论证明审阅稿可被批准为 R1，但不代表阶段 0 已完成或实现已启动。
- 路线图确认后的旧文档治理独立复审保留首红 P0=0、P1=1、P2=0：根 `AGENTS.md` 仍可从 0.2.0 阶段 0 派工。现已校准当前 Preview、冻结反向依赖与 Project Plan 清理事实，并加入 `WRC-0.4.0-R1` 等待目标模式门禁；定点复核最终 P0=0、P1=0、P2=0。

### 0.4.0 阶段 A（当前）

- 首轮源码/测试盘点复用了 watcher barrier、Changes/History/recovery/Safe Undo、native helper 构建链、Image/Markdown Trash 和真实 Electron harness 的既有边界；明确禁止把 project-hash 截断摘要/64 MiB worker、author acceptance copy、Diagnostic Export 或 Image Trash 直接包装成 Snapshot。
- 三条独立只读 lane 发现 P0=0、合同/实现接口 P1：Stage 0 未定义 snapshot stage/quarantine/delete identity payload 与 transaction nullability；现有 Changes/History 不支持恢复当前缺失的 Markdown leaf 或 Safe Undo exact 删除，且 64 文件/20 MiB 实现上限无法闭合合同 300 文件/64 MiB；local-task 枚举和 restore terminal result 未冻结；普通 watcher flush 也不能在 snapshot 自有 lease 下提供 same-scan bytes/图片/ancestor authority。
- 已在 `docs/EVIDENCE-DELIVERY-V1-CONTRACT.md` A-R1.1 冻结 private parent/stage/published/quarantine identity、create/delete 逐状态 nullability、owner-aware watcher drain + native same-scan barrier、local-task 枚举、restore terminal envelope，以及 History v4 的缺失 leaf/no-clobber/Safe Undo exact identity、canonical base64 和 192 MiB hard cap。相关 native/公开 Markdown mutation 在窄复审 P0/P1 清零前暂停。
- 当前最小纯逻辑 checkpoint：新增 `src/shared/evidence-delivery-schema.js` 与 `src/main/snapshot-bundle.js`，hostile Unicode/getter/prototype pollution/整数/digest/state matrix 11/11 通过，bundle magic/manifest/entry/footer/full SHA-256/corruption/trailing bytes 5/5 通过。这只证明 schema/reference parser，不是 native storage、App UI 或阶段 A 完成。
- A-R1.1 独立窄复审首红为 P0=0、P1=7、P2=1：manifest 自报 limits 可放宽、图片引用关系未闭合、U+007F 与合同漂移、digest schema getter 可先执行、cancelAvailable 矩阵未实现、delete/recovery/receipt/identity/restore-result hostile validator 不足，以及 exact snapshot bytes 与 History v4 对非法 UTF-8 语义未冻结；P2 为 parser 返回的 manifest 嵌套对象未深冻结。该 NO-GO 结论保留，后续绿灯不得覆盖。
- A-R1.2 已冻结：Markdown snapshot 候选必须是 fatal-decoder 可接受的严格 UTF-8，History v4 base64 保存并逐字节恢复 exact raw bytes；manifest limits 必须逐字段等于合同常量，图片 references 只可指向同 manifest Markdown，U+007F 明确合法，parser 读取 schema 前拒绝 accessor，取消可用 stage 明确枚举。待补齐 validators/hostile fixtures 并由同一独立复审员复核后，才能启动 native/storage mutation。
- A-R1.2 定点复审确认 fixed limits/reference/U+007F/getter/cancel/strict UTF-8/deep-freeze 已关闭，但保留新一轮 P0=0、P1=4、P2=1：最大 History fixture 仍用调用方 8 MiB envelope 而非完整 `changes.json` exact schema；公开 restored leaf 被误套私有 `0600`；root/ancestor/object、parent role 和 COMMITTED receipt authority 未闭合；隐藏 restore path 可通过；P2 为非终态 task 接受 `stage=completed`。当前 A-R1.3 只修这些协议点，继续保持 NO-GO。
- A-R1.3 focused checkpoint 已把完整 `changes/v4` document/application/provenance template、最大合法 path/summary/300 文件/128 MiB accountant、public restored-leaf mode、root/ancestor/object identity、parent role、COMMITTED receipt、隐藏路径和 status/stage 矩阵写成 executable validators；schema 21/21、bundle 10/10、四文件 `node --check` 与 `git diff --check` 通过。该绿线仍只等待独立定点复审，不是 native/storage、App UI 或阶段 A 完成。
- A-R1.3 定点复审为 P0=0、P1=2、P2=0：restore-created validator 仍允许省略 parent；所谓 production `changes/v4` validator 强制单 entry，与既有 History append/FIFO/迁移保留冲突。A-R1.4 只将 parent 改为必填，并把单条最大 fixture proof 与允许 1–100 条、由既有 kind validator 先验证保留项的生产 envelope 分开；继续保持 NO-GO。
- A-R1.4 focused checkpoint：restore-created 无 parent fail closed；单条 proof validator 与 production 1–100 entry envelope 已分名分责，生产 envelope 强制调用既有 History kind validator callback 并对完整 canonical document 执行 192 MiB gate；schema 22/22、bundle 10/10、四文件 `node --check` 与 `git diff --check` 通过。独立复审签收前仍不授权 native/storage mutation。
- A-R1.4 独立定点复审最终 P0=0、P1=0、P2=0：强制 ancestor parent 与 History proof/production envelope 分责均关闭，native/storage mutation GO。该签收只解除 A-R1.x 协议门禁；snapshot helper/service/IPC/Renderer、真实 Electron 和阶段 A 均尚未完成。
- A1 当前实现检查点（尚未完成）：新增 Main-only `local-operation-service`，把 2 秒进度、10 秒后有限取消、stage deadline、project invalidation 和 owner generation 收敛到一个 owner；focused 6/6 通过，覆盖旧 task/finally 不得释放或覆盖新 owner、commit stage 不可取消、timeout 不得伪造 `UNCOMMITTED`。新增 `snapshot-storage-helper` 的第一段原生边界，从可信文件系统根 fd 逐段 no-follow 绑定项目根和 `.writcraft/snapshots/v1/{control,bundles,quarantine}`，只实现 `create stage -> exact cancel -> zero publish`；focused 8/8、strict Clang 与临时 universal arm64+x86_64/macOS 11 签名校验通过。尚未实现 bundle publish、receipt/recovery、committed reconciliation 或 list，因此不能称为 A1 完成。
- A1 worker 检查点：新增 `snapshot-storage-worker`，开发态/打包态 helper 定位分离，绑定 root/private-parent identities，完整捕获和界定 helper 行协议，任何活动 stage 的未知 native write failure 进入 `recovery-required`，且在 awaited readiness/test hook 后再次检查 close state。focused 4/4 通过：schema-valid finalized stage + exact cancel 零 publish、await 后关闭不得写 child stdin、项目 pathname 替换后 fail closed、partial write 只能经 exact cancel 收敛。独立复审正在进行；这仍不是 service/IPC/Renderer、真实 Electron 或阶段 A 完成证据。
- A1 独立复审的首个实现红已保留：clean `worker.close()` 因先置 `closing=true` 而自拒发送 `X`（原测试 helper 吞掉该错误）；未 start 的 queued local task 在 deadline 后 signal abort 但仍占用 project owner；helper 对整个 stage 的 SHA 被误标为合同只覆盖 bundle payload 的 `bundlePayloadSha256`。前两项已定点修复并新增回归：close control request 是唯一允许在 closing 状态发送的命令，queued deadline 直接以可证明 `UNCOMMITTED/LOCAL_TASK_TIMEOUT` 终止并释放 exact owner；local-operation 现为 7/7。第三项已退回 native bundle parser author lane，必须用真实 bundle cross-check，当前不接受 arbitrary-byte fixture 作为闭合证据。
- A-R1.5 + capability 独立定点复核复现 schema 22/22、History 19/19、capability 8/8，核心无 P0/P1：普通 UTF-8 History 正文可保留 JSON-escaped C0，summary/metadata 仍严格拒绝；snapshot restore 可持久化 `undoneAt`，generic Undo 在任何读写前以 `SNAPSHOT_RESTORE_UNDO_REQUIRED` fail closed，专用入口只准备 CAS 审计。复审发现 capability hostile accessor 与无界 issued-ID 两项 P2；现已改为 owner-first exact validation 与进程随机前缀+全局单调计数，新增 direct authority drift/accessor/2048 次有界性回归，focused 9/9、schema 22/22。created-leaf 的 exact quarantine/write transaction 仍是后续集成门禁。
- native/storage 内核新一轮首红与定点证据已保留：持久化私有 stage reservation 后，旧测试先因 control 文件数由 2 变 3 而红；修复后 S 回包丢失、stage→marker unlink、marker→directory fsync 两个 UNKNOWN 可重试边界、late replacement 保护均通过。helper 21/21、worker 6/6、strict Clang 和 ad-hoc signed universal arm64+x86_64/macOS >=11 构建通过；source SHA `3c2373b8ef96f34547cd5a78fcaf100304da5e0b4dfc73835f3c523e1b89c3a3`，binary SHA `da5896e57911632058327dd6d17fbe1a0a6bd492645cb6ee12c9a6040f7a3037`。该 reservation 只是过渡所有权记录，不能冒充冻结 transaction/v1。
- A1 production capture 审计发现新的合同 P1：旧合同要求 native 决定 Markdown 引用图片闭包，却没有冻结图片 token authority；直接写 native 图片正则会成为第二套 parser。合同已先同步为两阶段 sealed capture：native 在同一 root-bound scan 封存 exact Markdown bytes/identity/digest 并签发 request-local opaque candidate ID，Main 只对这些 exact bytes 使用共享的唯一 `marked v18.0.6` tokenizer并回传 candidate token result，native 核验后复制图片/组 bundle，发布前再重枚举并重检全部来源。该修订和 production 实现尚待独立复审，因此阶段 A 仍为 NO-GO。
- 2026-08-08 durable create milestone：shared `marked v18.0.6` 保持唯一字节源；native-owned sealed capture 与 image token pass、root/ancestor/source recheck、formal transaction/no-clobber/receipt、COMMITTED/UNCOMMITTED/UNKNOWN reconciliation 已接入 production worker 和 Main create service。JS 在 fresh R 前等待旧 helper 确认退出；native R 在返回 UNCOMMITTED 前最后重检 late final/receipt/transaction。真实双 helper 首红证明旧 R 无法识别 late durable commit；修复后 exact commit 返回 COMMITTED，foreign replacement 返回 UNKNOWN 且证据保留。独立复审范围内 P0=0、P1=0、P2=0：native 37/37、worker 13/13、service 16/16、destroy latch 3/3、tokenizer 16/16、barrier 9/9、watcher 31/31，native source SHA `1afa171a6dc145d6a0d9d815f4602eb0f689d8fc01322619db9ec78779c6e5bc`。该签字只覆盖 create 底层，不代表 Main/IPC/Renderer App 功能或阶段 A 完成；compare/restore/delete、artifact lifecycle、真实 Electron、全量回归和作者验收仍是明确阻断。
- 2026-08-08 durable History artifact milestone：Changes/History 的大恢复记录使用小 control + hash-bound artifact，并由 production native lifecycle 完成 trusted-root bind、held read-only fd 校验、exact quarantine/cleanup/reconcile/ACK。独立复审确认 ACK 从 canonical control 重建 proof/receipt 关系，receipt/proof/control 三类 self-hashed foreign replacement 均拒绝且保留；fd4 在任何 control access 前强制 `O_RDONLY`、regular、expected/effective uid、`0600`、`nlink=1`。该窄层 P0=0、P1=0、P2=0：artifact 19/19、Snapshot recovery 13/13、ordinary recovery 24/24、History 20/20；source SHA `a4c4b077d8b9ab6b313436a1f8dc606842d83d2a12021d62ad6e996af8358139`，built-helper SHA `8242690ebfeaa8189cb3c7acf79bc8d8cd979b758caafec13a94cbaa30fadb6b`。它不替代公开 Markdown created-leaf Safe Undo 的原生四阶段 quarantine，也不证明 Snapshot helper package、App、真实 Electron 或阶段 A 完成。
- committed read/list 首轮独立复审保留的 NO-GO 为 P0=0、P1=2、P2=1：`O` 流后未重新证明 exact receipt；`L` hash 后未重开 final pathname，且同样未终态复验 receipt。修复后又由同 reviewer 复现 terminal 新增第二份 canonical matching receipt 时仍 `O OK` 的 P1，最终 terminal validator 改为重扫 control、要求 exactly one matching receipt，并 exact 对齐 basename/held identity/canonical record/digest；leaf/receipt 的 late delete、same-inode rewrite、replacement 与 duplicate hostile 均 fail closed且不删 foreign。最终该存储窄层 GO：P0=0、P1=0、P2=1，native 49/49、worker 18/18、bundle 10/10，source SHA `6b3e2048059b9f4b568e600c20bb517117b7440313d99e05c52906ee75d0a5d4`。P2 仍为最大 520 MiB bundle、约 1.04 GiB hex wire 的 RSS/通用 30 秒 helper timeout缺少真实容量闭包。
- snapshot comparison 首轮独立复审保留的 NO-GO 为 P0=0、P1=6、P2=0：count/byte、同步 deadline、release 后 owner、双 authority、错误脱敏和 cache exact cleanup 均有真实反例。第一轮修复后复审又保留 same-project ownerGeneration、post-resolve expiry/eviction 与 old timer replacement；最终 `assertFinalOwner` 绑定 exact owner+project，`readDiff` 在 shared lease 下对 context/live capability 与 snapshot/current authority逐 await 复核，旧 expiry callback 只可清 captured context。cooperative O(n) line diff 保留 final newline、多段变化和 30k 行跨页行号；retained/build peak 精确为 264/672 MiB。该 Main-only compare/capability 窄层最终独立签收 P0=0、P1=0、P2=0：compare 21/21、capability 9/9、schema 22/22、local operation 7/7、barrier 9/9；尚未接 production readers、IPC 或 Renderer。
- Main-only existing-leaf restore 首轮独立复审保留 P1=5/P2=1，随后又以真实 transaction normal/warning shape、聚合 clone-before-budget 和 cross-shape matrix 反例多轮复核。最终 service 使用 descriptor-only exact normalizer、executor exactly-once、post-attempt single marker reconciliation、committed-safe envelope、path/body-free error，以及 64/128 MiB fail-before-clone/base64 accountant；真实 normal/recovered/committed-warning 均不误触 service R。该 existing-leaf 窄层最终独立签收 P0=0、P1=0、P2=0：Restore 34/34、recovery 13/13、barrier 9/9、History 20/20。missing leaf 继续在 capability consume/marker/History/正文前以 `SNAPSHOT_RESTORE_CREATED_IDENTITY_UNAVAILABLE` 零写阻断，因为 mandatory `createdIdentityDigest` 只能在 native O_EXCL held fd 后生成；Safe Undo 的原生四阶段 quarantine 同样未完成。
- missing-leaf/Safe Undo 四阶段合同与纯 phase schema 的三轮独立复审先后保留：descriptor/accessor/sparse 与 mixed existing+missing P1、未绑定父 selection 完整 action/path/revision/ancestor mapping 的 P1，以及 insertion-order `JSON.stringify` 冒充全局 canonical digest 的 P1。最终 schema 从父 marker exact mapping 推导完整 MISSING/CREATED 子集，拒绝 existing 伪造 created identity与遗漏 missing，并唯一复用 `evidence-delivery-schema.digestObject`；独立 Unicode canonical preimage 的 SHA-256 与模块一致。该合同/schema checkpoint 最终 GO：P0=0、P1=0、P2=1，focused 11/11；P2 为 `operationId`、`artifactDigest`、`kind`、`preparedHistoryDigest`、`finalReceiptDigest` drift 尚缺逐项永久 fixture。它没有实现 transaction/reconciliation、native public-Markdown helper、missing restore 或 Safe Undo。
- public-Markdown PRECREATE 初版 3/3 后，独立复审保留 restart 未校验 artifact、parent selection 与 History template selected-ID 未 CAS，以及 aggregate budget/getter-order 三项 P1。修复后同一 Changes/History marker 与同一 WRCCHRA2 artifact 持久化 immutable History template；restart 在返回 applying 前 held-fd 校验 exact inode、whole SHA、control/base/template canonical digest，selection/template 按 ID/顺序/路径/动作/revision 完整 CAS，64/128 MiB 聚合预算在打开/编码/复制前执行，所有输入 descriptor-first getter-zero。该 PRECREATE checkpoint 独立签收 P0=0、P1=0、P2=0：transaction 7/7、phase schema 11/11、History 20/20、Snapshot recovery 13/13、legacy recovery 24/24、artifact lifecycle 19/19。它未进入 native receipt、History materialization/commit、FINALIZED 或完整 Safe Undo。
- missing restore `CREATED_RECEIPT` 初版 9/9 没有先执行独立首红，永久记录为证据纪律 P2；独立复审随后两次复现 marker 同字节换 inode与真正 `beforeMarkerRename` 窗口内 artifact/root 漂移仍可推进的 P1。最终 transaction 在 receipt 后及 atomic marker rename 临界动作内依次绑定 captured canonical root、project/ancestor/created receipt authority、artifact held identity/whole SHA/control/template 与 held marker authority；replacement、same-inode rewrite、ancestor/root replacement均保持 PRECREATE并不删 foreign。typed receipt、完整 mixed History materialization digest、response-loss 只 reconcile 与 create exactly-once 同样闭合。该 checkpoint 最终独立签收 P0=0、P1=0、P2=1：transaction 11/11、phase schema 11/11、History 20/20、Snapshot recovery 13/13、Changes recovery 24/24、artifact lifecycle 19/19。P2 仅为该 delegated checkpoint 未先跑首红的不可反向修补记录；尚未写磁盘 History、FINALIZED 或 Safe Undo。
- native public-Markdown private schema 在写 C 前先因合同缺失停下：首红 `rg` 证明 control/receipt/final-ack 与 wire 尚未冻结。补齐 exact private schema、deterministic basename、3.1.4 digest、CREATE/RECONCILE/FINALIZE_CREATE wire、三态与预算后，独立复审又保留 command 串线与 finalize 可省略 original create authority 两项 P1；最终所有 result 强制 expected command，所有 finalize builder/validator/encoder/ACK 强制完整 original create request并重算 deterministic names/digests。该合同/private schema checkpoint 独立签收 P0=0、P1=0、P2=0：native schema 9/9、evidence schema 22/22、phase schema 11/11。它不代表 native C/helper/adapter 已实现。
- production public-Markdown CREATE/RECONCILE/FINALIZE helper 的多轮真实 syscall 复审先后保留 leaf/ancestor 终态重检、FINALIZE record→ACK 窗口、UNCOMMITTED expected-name residue、partial current control 以及 cleanup 不绑定本次 O_EXCL-created inode 等 P1。最终 write failure 持有本次 created fd 与完整 identity 到 exact quarantine/cleanup；same-inode rewrite 或 same-bytes new-inode replacement 均只返回 UNKNOWN 并保留 foreign，只有仍绑定 exact created identity/canonical bytes 的 control 才可清理并证明 UNCOMMITTED。该 native helper/scoped adapter 窄层最终独立签收 P0=0、P1=0、P2=0：focused 55/55、native schema 9/9、evidence schema 22/22、phase schema 11/11、artifact lifecycle 19/19，arm64/x86_64 strict Clang、universal lipo 与 analyzer 均绿。它尚未注入 transaction/Main，也尚未进入 package/App。
- production public-Markdown adapter bridge 首轮独立复审以 nonzero/signal/error 仍信任合法样式 stdout，以及 CREATE 已尝试后 fresh R unavailable 被误报为 pre-mutation unavailable 两项 P1 阻断。修复后任何 C/R/F terminal 都先要求进程正常退出；CREATE 一旦尝试只可 fresh R，R 的 throw/nonzero/signal/malformed 均收敛为 path-free `CHANGES_MANUAL_RECOVERY_REQUIRED`/UNKNOWN 且零 CREATE replay，F 只重试同一 deterministic wire。该 bridge 最终独立签收 P0=0、P1=0、P2=1：native lifecycle 62/62、transaction phase 40/40、native schema 9/9、phase schema 11/11、evidence schema 22/22、artifact lifecycle 19/19、restore recovery 13/13。P2 为 JS `boundProject()` 尚只能以 absolute `openSync` 捕获 expected root/recovery identity；native mutation 仍由 trusted `/` fd + no-follow chain 保护，后续须由 native bind attestation 或等价 descriptor authority 关闭。transaction/service/Main/package/App 未包含在该签字中。
- missing restore `HISTORY_COMMITTED` 从最小 12/12 经过多轮独立 hostile：identity accessor、History rename 后 parent fsync EIO、同字节新 inode 冒用旧 durability、restart 不补 recovery-dir fsync，以及 fsync 后 project/root/ancestor 漂移均曾作为 P1 阻断。最终 prepared History file/parent authority 持有到 marker rename；restart 对 exact History parent 与 recovery directory补 fsync并做 before/after identity/raw-byte复核，project ID、root、所有 selected public ancestors、marker、artifact 和 prepared History 均在承认 phase 前重新绑定。query 对 prepared/base/absent/foreign 分别自动收敛、保持可继续或 manual recovery，adapter 调用为 0。完整 HISTORY_COMMITTED 层独立签收 P0=0、P1=0、P2=0：transaction phase 22/22、phase schema 11/11、History 20/20、Snapshot recovery 13/13、Changes recovery 24/24、artifact lifecycle 19/19。FINALIZED 与 Safe Undo 仍未完成。
- missing restore `FINALIZED` 从 minimal 23/23 起先后关闭 forged final ACK、marker rename 前 final record 漂移、foreign cleanup token、ACK committed-then-response-loss 丢失 retry authority、Main path-unlink marker 与缺 production marker adapter 后置检查等 P1。最终 fake/injected transaction 层从 formal final record restart 自动 H→F，FINALIZED marker durability、cleanup token/response loss、artifact cleanup exactly-once、marker exact clear/ACK 与 UNKNOWN 保留均闭合；缺/partial/accessor marker lifecycle 在任何 cleanup/CAS 前 `MARKER_CLEAR_UNAVAILABLE` 且零写。该 injected FINALIZED cleanup 层独立签收 P0=0、P1=0、P2=0：focused 39/39、artifact lifecycle 19/19、phase 11/11、History 20/20、Snapshot recovery 13/13、Changes recovery 24/24、native schema 9/9。production exact-marker native lifecycle 与 wiring 仍未实现，故完整 App 路径尚不可用；Safe Undo 同样未开始。
- production exact-marker 在写 C 前先冻结独立 private schema：request/control/receipt/token/result/ACK、确定性 record names、统一 3.1.4 digest、96 MiB marker 上限、CLEAR/RECONCILE 三态与 mandatory original authority。首轮独立复审以 `ACKED` 可携任意 raw error、Control/Receipt/Token 可无父 authority 自证两项 P1 阻断；修复后所有 authority builder/validator 强制 original request（Receipt 还强制 request-bound Control），`ACKED` 仅允许 `errorCode=null`、`UNKNOWN` 仅允许固定 `MARKER_CLEAR_UNKNOWN`。该合同/schema checkpoint 最终独立签收 P0=0、P1=0、P2=0：marker schema 8/8、artifact lifecycle 19/19、evidence schema 22/22。production native C/scoped lifecycle 与 Main wiring 仍未实现，不能据此宣称 marker 可安全删除。
- production exact-marker source/lifecycle 首轮 18/18 后被独立复审以三个跨进程 authority P1 阻断：CLEAR 可消费预置自洽 control；ACK 会删除同字节 foreign record replacement；CLEAR→fresh R/ACK 未绑定初次 root/recovery。修复中又发现 receipt-only partial set 会在返回 UNKNOWN 前错误创建 control。最终 request/token 绑定 trusted-root/project-chain/recovery 与 control/receipt publication identities；CLEAR 只接受本次 `O_EXCL` 创建且在任何 mutation 前证明 control+receipt 同时 absent；fresh R/ACK 重算初次 chain 并只处理 exact publication identity，partial/foreign 保持 UNKNOWN 且零写。该 source/lifecycle 最终独立签收 P0=0、P1=0、P2=4：marker schema 9/9、native lifecycle 29/29、artifact lifecycle 19/19，双架构 strict/universal/lipo 与独立 replacement 矩阵均绿。P2 为 analyzer 的 4 个既有诊断（一个 `openat` segment 误报、两个测试宏 dead store、一个非终端 errno 分支）；transaction/Main/package 未包含在签字中。
- full-missing Restore production orchestrator 首轮 42/38 后，独立真实集成保留 History template ID shape 不兼容、direct mixed 可伪报 existing applied，以及 absent leaf 伪造 non-null created identity 可绕过 mixed gate 三项 P1。最终 transaction 唯一投影 frozen top-level `historyEntryId`，service 不再探测 nested fake shape；所有 pre-native `createdIdentityDigest` 严格为 null，missing 仅由 `before.exists=false` 判定；full-missing 走 PRECREATE→一次 CREATE→History durable→FINALIZE→exact clear，response loss只 fresh R。该窄层最终独立签收 P0=0、P1=0、P2=0：phase 44/44、service 39/39、restore recovery 13/13、phase schema 11/11、History recovery 24/24；真实 production adapter 提交为 COMMITTED、CREATE=1、History/cap consume/lease release/marker clear各一次且无 pending。mixed existing+missing 目前在 transaction/service 最早边界零写拒绝；因为缺 created-leaf rollback receipt与 existing-write formal reconcile/rollback authority，Stage A 仍保留 mixed all-or-nothing P1，不能把安全拒绝称为功能完成。
- Safe Undo readiness 审计以私有协议、native/lifecycle、PRECREATE transaction、QUARANTINED 后 restart/History/finalize 四项 P1 要求 stop-before-C。现已先冻结同一 helper/recovery 下 Q/R/B/D/A、完整 original authority、CREATED ordered subset、root/artifact/History/phase binding、正式 record identities、三态/nullability 与 partial/foreign untouched；首轮独立复审只留 full-wire golden/最大 Unicode 预算未进 focused 的 P2，补入独立硬编码 Q/R/B/D/A SHA、字节数与 300 项 4087-byte path fixture后，合同/纯 schema 最终签收 P0=0、P1=0、P2=0：Safe Undo schema 10/10、create schema 9/9、phase 11/11。native C/lifecycle/transaction 仍未实现，`SNAPSHOT_RESTORE_QUARANTINE_UNAVAILABLE` gate 保持。
- mixed existing+missing all-or-nothing 设计审计确认 Safe Undo 只能复用底层 quarantine 原语，不能复用其 `snapshot_restore_undo` 协议 authority；必须新增 formal existing executor 与独立 `ROLLBACK_CREATE` domain。现先在同一 marker 的高层 phase 合同/schema 冻结 full-missing 直达主线、mixed `CREATED_RECEIPT→EXISTING_COMMITTED→HISTORY_COMMITTED` 主线，以及 formal existing UNCOMMITTED 后唯一 `CREATE_ROLLBACK_QUARANTINED→ROLLED_BACK` 支线；`existingReceiptSetDigest`/`rollbackReceiptDigest`、UNKNOWN锁定、跨支线/倒退/replay和完整 300 MISSING subset 均由纯 schema CAS。首轮独立复审仅留测试误用交替集合只覆盖150 derived items的 P2，补入独立300全MISSING/301拒绝fixture后最终签收 P0=0、P1=0、P2=0：phase 14/14。existing executor、ROLLBACK_CREATE private schema/native/transaction仍未实现，mixed继续 `SNAPSHOT_RESTORE_MIXED_SELECTION_UNAVAILABLE` stop-before-native。
- formal EXISTING executor 合同/schema 冻结 execute/reconcile/verify/finalize/reconcileFinalize 五方法与 E/R/V/F wire，绑定同一 marker/artifact/CREATED_RECEIPT、raw base History、完整 ordered EXISTING subset、before/after bytes/revision/ancestor/leaf identity；COMMITTED 要求完整 apply receipts，UNCOMMITTED 可含 partial apply但必须有完整 self-rollback receipts，UNKNOWN不铸 authority。首轮复审以 apply/rollback token 可对同一 canonical record声明不同 publication identity 的 P1和 drift fixture误传参数的P2阻断；修复为完整 canonical apply authority CAS并逐项正确命中 marker/artifact/phase/selection/History drift。ROLLBACK_CREATE native 开工前又发现 leaf identity 只是 regex-valid digest、native 无从独立重建的 P1；现追加唯一 `writcraft.public-markdown-existing-leaf-identity/v1` canonical payload，绑定 selectedId/path/revision/ancestor、dev/ino/uid/mode/nlink/size/mtimeNs/ctimeNs/content SHA，并以 uint64 canonical decimal、size/content/revision 闭包和 global 3.1.4 digest拒绝 same-byte new inode、same-inode rewrite与跨publication伪造。该合同/schema最终签收 P0=0、P1=0、P2=0：existing 18/18、ROLLBACK_CREATE 14/14、phase 15/15、CREATE 9/9；native/lifecycle/transaction尚未实现。
- Safe Undo native/storage Q/R/B/D/A 首轮17/17后，独立复审复现本次Q自行 O_EXCL 创建的 current control partial write残留并误入UNKNOWN的P1。最终 `RecordAttempt` 持有本次created fd、完整identity与写入prefix；只有current path/held inode/metadata/canonical attempted bytes全exact才可随机隔离、重开验证、unlink并fsync，late same/new inode、相同prefix重写、preexisting/foreign均UNKNOWN保留。该native/lifecycle最终签收 P0=0、P1=0、P2=0：undo 26/26、既有CREATE 62/62、undo schema10/10、phase14/14、native schema9/9，双架构strict/universal/lipo/analyzer全绿。transaction/service/Main/package尚未接入。
- 独立 `ROLLBACK_CREATE` private 合同/schema 已冻结 Q/R/D/A（明确无 B）与 `CREATE_ROLLBACK` 分域：绑定同一 marker/artifact、完整 CREATED_RECEIPT MISSING authority、CREATE publication identities、formal EXISTING UNCOMMITTED receipt-set 和 exact raw History base。首轮复审发现 A 只核顶层 digest、未逐项 CAS created/creation/quarantine receipts 的 P1，以及 300 项 fixture 实际 artifact range overlap 的 P2；修复后 A 从 original phase、全部 Q token、formal existing set 与 D final 重建完整 expected ROLLED_BACK。后续 native 开工前复审又两次阻断仅传 digest、无法跨进程重证 authority 的缺口；合同现明确 fd3 trusted `/`、fd4 artifact、fd5 marker、fd6 History parent、fd7 present History file/absence directory，Q/R/D/A wire 携完整 ordered E/I authority，D/A 再携 T tokens，并补齐 CREATE precreate phase、每项 artifact offset、prepared History、原 CREATED_RECEIPT timestamp 与 A 新 timestamp 单调链。focused 从完整 wire 独立重建 CREATE control/receipt publication、CREATED_RECEIPT 与 ROLLED_BACK，不以 supplied digest 自验。再次进入C前的双重审计又拦下 supplied precreate/selection digest仍不可独立重算，以及最大合法299-token COMMITTED响应超过256KiB的两个P1；最终authority纳入exact original PRECREATE，header携两枚原timestamp，E/I以唯一parentIndex恢复完整mixed顺序，focused只从parsed wire重建selection、PRECREATE与CREATED_RECEIPT；ROLLBACK_CREATE单独冻结512KiB response envelope并以299 K的Q/R 274527-byte hardcoded SHA、overflow/stderr/非法UTF-8/303行拒绝闭合，generic CREATE/Undo仍保持256KiB。该合同/schema最终独立签收P0=0、P1=0、P2=0：rollback16/16、CREATE9/9、Safe Undo10/10、EXISTING18/18、phase15/15。native/lifecycle 尚未实现，mixed transaction继续零写拒绝。
- Snapshot History ancestor + Safe Undo `RESTORED` phase 已独立签收 P0=0、P1=0、P2=0：History 22/22、phase 15/15、Safe Undo schema 10/10、ordinary recovery 24/24。`snapshot_restore` 每项 `ancestorIdentityDigest` 必须为 non-null SHA-256并进入 integrity；ordinary 新 v4 必须为 null，pre-ancestor ordinary v4 只在旧 payload/integrity 先验证后内存迁移并重写，旧/新 tamper 均拒绝。template→materialize→save→restart→undo 全程保留 ancestor authority；Safe Undo 仅允许 `QUARANTINED→RESTORED` 作为 History 提交前 B 成功的正式终态，保留 created/quarantine receipts 与 preparedHistoryDigest、要求 B final receipt、无 outgoing 且不声称 History 已 undone。transaction caller 尚未把 parent selection 的 sealed ancestor merge 到 History files，现有 `INVALID_HISTORY` 首红作为下一 Safe Undo transaction 接线 P1 保留。
- Safe Undo transaction 的最小 Q/R 层首轮47/47后被独立复审以四项 P1 阻断：公开入口仍依赖外部 parent binding、顶层 accessor/extra 未 descriptor-exact、Q 前未绑定当前 raw History、QUARANTINED fresh R 后未重验 root/project/artifact/marker。修复后入口仅以 exact `rootPath/projectId/entryId` 从 sealed applied Snapshot History 重建完整 parent/action/path/revision/ancestor；raw History authority 跨 Q、首个 R 与 CAS 第二个 R 持有，合法重签 ancestor、仅 minify raw、same/new inode 与 R 回调内 root/project/artifact/marker 漂移均 fail closed。attempt latch 失败 Q=0，response loss/UNKNOWN/retry 的 Q 总数始终为1。该窄层最终独立签收 P0=0、P1=0、P2=0：transaction 54/54、History 22/22、phase 15/15、Safe Undo schema10/10，另引用 production undo lifecycle26/26 的 Q/R record replacement 证据。签收范围止于 `entryId→PRECREATE→Q/fresh-R→QUARANTINED query`；B/D、History commit、terminal cleanup 与 Main 尚未实现。
- Safe Undo B restart 审计发现完整 Q tokens 仅存于内存，若 `RESTORED` CAS 后、A 前崩溃便无法收敛；拒绝同步窗假闭环后，在同一 existing marker 内冻结 private settlement authority，不新增 sidecar。首轮 pure schema 又因只能分别验证 null/PREPARED、缺 previous→next marker CAS gate而以 P1 阻断；最终统一 transition API 只允许 QUARANTINED null→PREPARED、QUARANTINED PREPARED→RESTORED COMMITTED和RESTORED exact replay，drop/rewrite/skip/cross-phase均拒绝。settlement 持久化完整 ordered Q tokens/publication identity、deterministic B/final/A authority，300项454311 bytes低于512KiB。该合同/schema 独立签收 P0=0、P1=0、P2=0：settlement7/7、native undo10/10、phase15/15、transaction54/54、History22/22。
- Safe Undo B→RESTORED transaction/restart 初版58/58后先发现 B/A pre-CAS proof 内嵌retry可把每阶段调用放大到4次的P1，继而独立复审又复现 post-adapter gate只比较path/projectId、没有重算exact root/recovery identity的P1。最终 PREPARED settlement 必须先持久化才允许B；B/A primary/retry加single-call pre-CAS proof全路径最多3次且wire byte-identical；每次Q/R/B/A callback后都从当前root/recovery重新构建完整native authority并与captured authority canonical exact比较。same-path project root/recovery replacement、marker/artifact/History drift与A UNKNOWN/response-loss均保留双树foreign及可重启PREPARED/RESTORED权威。该checkpoint独立签收 P0=0、P1=0、P2=0：transaction67/67、settlement7/7、native undo10/10、phase15/15、History22/22、artifact19/19、marker29/29。签收范围止于B rollback、RESTORED terminalize与exact cleanup；Safe Undo History commit、D/FINALIZED、Main尚未实现。
- Safe Undo History commit 开工前发现现有 WRCCHRA2 只持久 ordinary restore template、`undoneAt` 与 exact prepared undone History仅在进程内，重启无法确定性重建的P1并立即stop-before-write。现于同一 artifact `preparedDelta` section 内冻结独立 `writcraft.snapshot-restore-undo-history-template/v1`，exact包含status=undone、appliedAt/undoneAt、files/provenance/ancestor与baseHistoryState；raw base digest验证后materializer确定性生成exact undone History bytes/digest，restore/undo template由schema区分且v1/ordinary兼容不变。该template/materializer独立签收 P0=0、P1=0、P2=0：undo-template6/6、History22/22、artifact19/19、phase15/15、native undo10/10、transaction67/67；hardcoded canonical/3.1.4、restart/tamper/cross-kind、300/301与32MiB pre-copy gate均闭合。transaction尚未据此提交History。
- Safe Undo QUARANTINED→HISTORY_COMMITTED 使用artifact inspect返回的validated undo template与exact raw base确定性materialize undone History；settlement必须null，fresh Q-R前后均须COMMITTED且public quarantine保持。History temp/file fsync/rename/parent fsync、held file/parent、marker CAS与restart base/prepared/committed分支均沿同一marker/WRCCHRA2闭合，B/D/A调用为0。首轮独立复审发现HISTORY_COMMITTED restart只看可见marker bytes、未补recovery-directory fsync的P1；修复后强制复用exact marker durability ACK，真实marker rename已提交+首次fsync失败会跨restart对同一dir第二次fsync，连续失败保持locked，marker/root/recovery replacement fail closed。该层独立签收 P0=0、P1=0、P2=0：transaction75/75、template6/6、History22/22、artifact19/19、phase15/15、undo10/10、settlement7/7、marker29/29。D/FINALIZED、terminal cleanup与Main尚未实现。
- Safe Undo D 开工前发现FINALIZED只保留final digest，A exact删完records后响应丢失便无法重建ACK authority的P1并stop-before-D。现于同一marker冻结独立 `publicMarkdownUndoFinalization`：HISTORY_COMMITTED null→PREPARED持久完整D request/Q tokens，D COMMITTED→FINALIZED COMMITTED再持久full final identity与exact A request，FINALIZED只允许byte-exact replay；drop/rewrite/skip/cross-B/timestamp/stale均拒，A committed-loss不依赖fresh D。该合同/pure schema独立签收 P0=0、P1=0、P2=0：finalization7/7、settlement7/7、undo10/10、phase15/15、template6/6、History22/22、artifact19/19、transaction75/75；2-item hardcoded canonical/SHA与300项454582/455531-byte预算闭合，301拒绝。transaction尚未据此调用D/A。
- Safe Undo D/A transaction 初版83/83后，相邻真实 native 回归暴露暂停中的 `ROLLBACK_CREATE` parser 把约6.79MiB `RollbackRequest`放入`main`栈帧，与既有约1.5–1.63MiB嵌套帧叠加后触发SIGSEGV；修为分支内`calloc/free`后`main`降至约1.67MiB，并以`-Wframe-larger-than=2097152`永久门禁，native Undo26/26、CREATE63/63及双架构strict/analyzer恢复。D/A独立复审随后又阻断fake把Undo R伪装成CREATE `reconcile`、production high lifecycle未暴露Q/R/B/D/A的P1；窄修后high descriptor-safe公开完整两套方法，所有Undo调用统一`reconcileUndo`，真实high Q response-loss只接受exact `QUARANTINE`或low已fresh-R的`RECONCILE_UNDO`，而transaction仍自行fresh R后才推进。最终D/A production lifecycle/transaction/restart独立签收P0=0、P1=0、P2=0：CREATE63/63、native Undo26/26、transaction84/84、finalization7/7、settlement7/7、undo10/10、phase15/15、template6/6、History22/22、artifact19/19、marker29/29。`HISTORY_COMMITTED`先持久PREPARED Q/D authority；D与A各最多3次byte-identical调用，D loss不fresh Q，A committed-loss只用stored authority且不fresh D；FINALIZED durability、root/recovery/History/artifact/marker/final identity漂移、cleanup response-loss与B=0均闭合。签收范围止于SafeUndo transaction/restart；Main/IPC/UI、mixed rollback与发布状态仍未完成。
- `ROLLBACK_CREATE` native/lifecycle Q/R 最小层先以真实 1 EXISTING + 1 MISSING、fd3–7 与正式 CREATE/EXISTING publications跑出并修复 schema literal、namespace、fresh-R终前CAS、Q public-rename前CAS与`RecordAttempt`四类P1，focused24/24、rollback schema16/16、CREATE63/63、SafeUndo26/26、双架构strict+2MiB frame+analyzer、node/diff均绿；但该签收随后被更深的跨进程复审撤回。原因是Q helper已提交并退出、Main尚未持久化token的响应丢失窗口内，fresh R只能从当前deterministic control/receipt重新捕获publication identity；same-byte new inode可被重铸为COMMITTED token。D final publication具有同一不可区分性，A又缺每条record cleanup前后的持久authority。现保持稳定Q/R代码checkpoint与D/A fixed UNKNOWN，但native/lifecycle整体NO-GO：须先冻结同一marker内、kind-scoped、exact previous-bytes/identity CAS的helper publication authority，既不新增sidecar也不放宽same/new inode边界，再重新独立签收Safe Undo与ROLLBACK_CREATE response-loss链。
- 为闭合上述跨进程publication identity缺口，同一 `changes-history-transaction.json` basename现冻结为永久stable-inode双槽journal而不再terminal unlink：`IDLE|ACTIVE`、u64 generation/previous-value链、WRCCHRJ2 canonical frame、publication `PREPARED→ARMED→COMMITTED→ACK_PREPARED→ACK_COMMITTED`、full-inode random stage authority、mutation arm、partial ACK cleanup与exact terminal cleanup truth均由pure schema约束。首轮12/12被独立复审以record子状态越级、slot head未执行完整transition、ordinal/identity/name碰撞、任意terminal digest与digest-builder getter五组P1阻断；窄修后journal17/17、phase15/15、settlement7/7、finalization7/7、rollback16/16，901-record hard golden为624849 bytes / `bc5a22ac3e20632b41440fb6d4523ba8337e4a454f146a4f1795e1aa7ab8c589`。该合同/schema最终独立签收P0=0、P1=0、P2=2：仅pre-ARM hard crash留下至多一个unreferenced private stage、以及stale/different journal clone导致的fail-closed manual锁定；两者均零public mutation、不收养/删除foreign且不宣称自动收敛。physical writer、legacy迁移、native publication与Main/transaction接线尚未实现。
- Permanent journal physical writer 的 in-process dependency protocol 已独立签收 P0=0、P1=0、P2=0：writer12/12、journal17/17。它强制 descriptor-chain verifier、held root/private/recovery identities、basename-relative nofollow opener、expected-head写前CAS与file/dir fsync后重验；crossed fd chain、symlink、stale head、same-byte replacement均fail-before-write或manual并保留foreign。该签收不冒充production ancestry：native `verifyDirectoryChain/openJournalAt` collaborator仍是实现门禁。
- Native journal physical-CAS private wire 冻结 `WRCCHJN2/WRCCHJO2/WRCCHJE2` INIT/READ/APPEND，startup fd3 trusted-root descriptor walk，完整WRCCHRJ2 logical value的96MiB hard transport cap和约192MiB physical cap。Main在encode前执行IDLE/完整business `assertTransition`；native只从physical header独立核journalId/generation/slot/previous-value/rawSHA/length与expected head，绝不复制nested business JSON parser。fresh READ只以exact old/next判UNCOMMITTED/COMMITTED，其余UNKNOWN；所有result/error绑定3.1.4 requestDigest与expected command/head。两轮独立复审先后关闭自产digest、forged request、非canonical count/NUL/getter、cross-error，以及C/Main分层冲突；最终该合同/schema独立签收P0=0、P1=0、P2=0：native wire9/9、journal17/17、writer12/12。96MiB fixture明确只是hard parser cap，当前最大合法business仍由901-record 624849-byte golden证明；production C/lifecycle尚未实现。
- Permanent journal 的 small native collaborator 已形成首个可运行、独立签收的 physical-CAS 窄层：真实 INIT→READ→APPEND→READ，统一 VALUE durability barrier（journal file fsync、recovery directory fsync、root/private/recovery rebind、nofollow fresh reopen、exact head 与最终 inode/path/chain CAS），direct APPEND 仅接受 exact next，old/UNKNOWN fail closed。复审发现并关闭 physical B 槽伪装自报 A 槽的P1；最终 lifecycle10/10、native wire9/9、journal17/17，arm64/x86_64 strict与2MiB frame gate全绿，P0=0、P1=0、P2=0。该签收仅覆盖 small collaborator，不覆盖96MiB实载、legacy/IDLE service迁移、publication业务状态机、package/Main/App。
- Service迁移前的字段来源审计发现，原永久journal `ACTIVE` 只保存`activeMarkerDigest`，同basename切换为WRCCHRJ2后无法在restart重建旧marker全文。纯合同/schema已补 descriptor-safe完整`activeMarker` preimage、13个必需及6个可选顶层字段、递归JSON/getter0、95MiB cap、3.1.4全文digest和ACTIVE generation CAS；journal只证明全文未换，既有reconciliation `validateMarker`仍是legacy integrity及业务语义唯一authority，避免第二套marker validator。纠偏后journal20/20、native wire9/9、writer12/12、native lifecycle10/10，独立签收P0=0、P1=0、P2=1；唯一P2是95MiB cap/+1尚无持久真实边界fixture，service接线必须以伪legacy integrity首红证明消费时确实拒绝。
- Fresh-process审计发现原READ必须由Main预知1–2个head，进程重启后无法构造请求；已新增physical-only DISCOVER：ABSENT、LEGACY、BASE（仅slot A/gen0/null predecessor）、PAIR（older完整value+相邻old/new heads）或UNKNOWN。PAIR后Main再以现有READ取newer并执行完整business transition，不把C升级为第二个JSON业务parser。DISCOVER pure wire11/11与真实C/lifecycle已独立签收P0=0、P1=0、P2=0：fresh ABSENT、INIT已提交但响应丢失后的BASE、APPEND后新进程PAIR→READ、legacy、non-base单槽UNKNOWN以及same/new inode replacement均实跑闭合；journal20/20、双架构strict/frame全绿。该层仍不等于service接线或96MiB实载签收。
- Reconciliation service的journal只读入口已独立签收P0=0、P1=0、P2=0：adapter依赖先descriptor gate；ABSENT/合法BASE-IDLE返回无pending，LEGACY继续走原JSON `validateMarker`，ACTIVE取journal中的完整preimage后仍必须通过同一legacy integrity与业务validator，UNKNOWN/malformed fail closed。复审发现并关闭伪BASE可携B/gen1/ACTIVE绕过分类的P1；最终focused8/8、legacy recovery24/24、node/diff全绿。当前仅签readMarker/hasPending；prepare/persist/clear、terminal IDLE与Main注入仍未接。
- Ordinary apply/review/undo 的 permanent-journal service闭环已独立签收P0=0、P1=0、P2=1：ABSENT→INIT IDLE→ACTIVE完整marker，finish以exact expected marker继续ACTIVE CAS，clear复核terminal/current History并以唯一`digestHistoryState`写terminalCleanup，再落永久IDLE；全程4次APPEND、零marker unlink/republish。INIT/APPEND结果严格绑定原request与合法fresh READ，response-loss不重放；History drift在cleanup前零clear append，fresh DISCOVER PAIR可证明紧邻ACTIVE→IDLE的同operation clear。focused19/19、journal read8/8、legacy recovery24/24、History24/24、journal20/20。唯一P2为：若新ACTIVE已显式启动，延迟的旧clear operation-specific成功回执只可manual/fail closed；新操作不被覆盖或删除。该实现尚未注入Main；Snapshot CREATE已迁至`CREATED_RECEIPT`，其History/finalize/cleanup及Safe Undo仍需permanent-journal迁移。
- Missing-leaf normal CREATE 的 permanent-journal capture/domain层已冻结并独立签收：initial PREPARED不授权native；唯一PREPARED→PREPARED generation先持久deterministic attempt latch，只有同一live call确认该latch后可调用一次CJ。helper成功后一次性安装ordered 1–300项capture，再以byte-stable `ARMED→COMMITTED`持久化；每项绑定selected/path/content/ancestor、created leaf full identity、creation receipt及control/receipt的logical digest、raw SHA、deterministic paired basename与`0600+nlink1` publication identity。attempt已落但capture尚未持久时的crash/response loss是整条CREATE路线唯一安全P2：固定`UNKNOWN/manual`，fresh R、restart与显式retry均不得重新捕获、铸造或重CREATE。
- CREATE_MISSING current-head wire/native/lifecycle已完成fd5-free迁移：CJ仅用fd3 trusted root与fd4 artifact；C从permanent journal physical head/frame/payload和五个canonical slices独立重算request、attempt latch、latched PREPARED publication、binding/slice-set/command token，不解析nested业务JSON。真实300-item最大字段fixture为395,434-byte响应、4,697,033-byte capture、4,699,529-byte frame，301拒绝；完整multi-item、control fault/cleanup、guard drift、transport/response-loss与300 success独立签收P0=0、P1=0、P2=1，P2仅near-cap timing evidence尚未持久化，安全P1为0。CJ focused26/26、pure13/13、legacy CREATE63/63、Safe Undo26/26、双架构strict与2MiB frame gate全绿。
- Snapshot permanent-journal PREPARE+attempt/capture service层已独立签收P0=0、P1=0、P2=0：PREPARE先以2次APPEND持久ACTIVE marker与initial PREPARED，CREATE live call再以1次APPEND latch attempt，native成功后以3次APPEND依次持久ARMED capture、COMMITTED publication及`CREATED_RECEIPT` marker，共6次。latch与每次APPEND response loss只用fresh exact READ收敛；入口已latched的restart/显式retry、native UNKNOWN retry、History/artifact/journal drift均不重CREATE，old create/reconcile调用为0。focused41/41、journal read8/8、recovery24/24、high lifecycle63/63、CJ26/26、pure13/13均通过。签收止于`CREATED_RECEIPT`；下一层是permanent-journal History commit，CREATE finalize/ACK cleanup、mixed EXISTING、Main/package仍未完成。
- Full-missing Snapshot permanent-journal History commit已独立签收P0=0、P1=0、P2=0：从exact `CREATED_RECEIPT`、byte-stable COMMITTED capture/attempt和immutable History template确定性物化prepared History，持有base/prepared file+parent authority并补fsync后，以第7次APPEND推进同一ACTIVE marker至`HISTORY_COMMITTED`。第7 APPEND response loss只fresh exact READ；prepared History可跨restart继续且CREATE仍为1；真实temp partial/file-fsync失败保持`CREATED_RECEIPT`，rename committed/parent-fsync response loss由同一parent durability retry收敛，foreign History及History durable后的artifact/project/current-head漂移均manual并保留证据。focused52/52、journal read8/8、recovery24/24、History24/24、high lifecycle63/63、CJ26/26、node/diff通过。CREATE finalize/ACK cleanup、mixed EXISTING与Main仍未完成。
- safe-delete storage 首轮作者专项虽为 14/14、native 49/49、worker 18/18，独立复审仍复现唯一 canonical create receipt 被改到另一个合法形状 basename 后 Y 错误提交删除的 P1。修复后 initial 与 terminal 两次 receipt 扫描均从 `receipt.transactionId` 派生 exact basename；moved-only/duplicate pre-Y 保持 final 且零 delete control/quarantine，rename 后 late moved-only 只可 Y/Z UNKNOWN 并保留 quarantine。该 native/worker 存储层最终独立签收 P0=0、P1=0、P2=1：delete 16/16、native 49/49、worker 18/18、strict Clang 与 Node syntax 通过；P2 是冻结合同已记录的恶意同 UID 精确 TOCTOU 残余。Main delete service、IPC/Renderer、App 与真实 Electron 均未包含在该签字中。
- Main-only safe-delete service 首轮 18/18 后，独立复审仍保留 capability adapter 未重验 fixed TTL/完整 authority digest，以及 fresh Z 已证明 COMMITTED 后无界等待 list/close 的两个 P1。最终 service 对 issued/peek/consumed authority 做 descriptor-exact、getter-zero、固定 5 分钟 TTL、schema/kind/singleUse/owner/project/snapshot 与 canonical selection/authority digest 对账；fresh Z 则先发布 terminal 并释放 exact lease，optional list/close 永不阻断且旧 cleanup 不触碰 replacement authority。该 Main service 最终独立签收 P0=0、P1=0、P2=0：service 22/22、capability 9/9、local operation 7/7、barrier 9/9、create 16/16、worker 18/18。仍未接 IPC、Renderer 或 App。
- native publish author lane 的中间态首次 strict Clang 红也已保留：新增 `sha256_prefixed`/`safe_timestamp` 尚未接线即触发 `-Werror,-Wunused-function`，导致同轮 worker 测试在编译边界停止；这不是产品回归绿灯，需待 publish/reconciliation/list 实现稳定后按同一 strict 命令重跑。
- History v4 独立 layer 已实现：`change-history-service` 当前写 `writcraft.changes/v4`，v1–v3 只读兼容并确定性迁移；普通 Changes 继续使用 exact UTF-8 state，snapshot restore 新入口只准备 exact base64 raw-byte before/after、created leaf identity 与 snapshot provenance，完整文档使用 100-entry FIFO/192 MiB gate，单条 proof 不冒充 production envelope。focused 19/19；Changes durable recovery 24/24、handler 10/10、workspace 7/7、Renderer recovery 16/16、ChangeSet review 15/15、Inline Rewrite History 6/6、Inline Rewrite integration 7/7、Research apply transaction 12/12，合计 116 项通过。此层未实现 Snapshot service/IPC/Renderer。
- History recovery 同轮首红保留：CAS foreign-state fixture 仍通过已从 v4 移除的 `updatedAt` 注入，实际制造的是无效 shape 而非合法 foreign state；测试改为注入合法空 `writcraft.changes/v4` document 后，CAS fail-closed 路径 24/24。该修订是测试权威更新，不是降低 product validator。
- History v4 独立复审随后发现 A-R1.5 合同/实现首红，当前保持 NO-GO：普通 Changes 的合法 Markdown 换行会被全局 canonical C0 禁令拒绝，因此 production envelope 实际未被非空 v4 调用；snapshot restore entry validator 又只允许 `status=applied`，使 Safe Undo 无法持久化 `undone`，含 created leaf 还会更早误入普通 ChangeSet null-text 路径。合同已冻结唯一窄例外：完整 v4 对验证、定序重建后的 plain data 使用无空白 JSON byte 计数，C0 只可出现在普通 `encoding=utf8` 的正文 data；snapshot entry 的 `undone` 必须保留 provenance/files/integrity 并增加 `undoneAt`，普通 Undo UI 必须路由 Snapshot 专用 identity/quarantine 事务。共享 gate、History validator 与专用 Safe Undo 未经独立复核前不得称闭合。
- 2026-08-08 已按“先搜再更新”再次更新并反查 Nowledge authority memory `fb4b3fbf-beed-4401-9bcf-07a123ea5682`：除 create 底层签收外，新增 History artifact lifecycle 的 P0=0/P1=0/P2=0 范围与哈希证据，并明确记录 committed read/list 当前独立复审仍为 NO-GO（P1=2、P2=1）、compare/restore/delete/App/真实验收未完成；旧“0.4.0 未启动”记忆继续不得派工，外部发布仍需单独授权。

### 0.3.0 发布前保留证据（已冻结）

- 统一任务条已完成基础 Renderer 接入；Chat/Navigation/Chapter/Research/Graph/Changes/图片作者路径和入口取消、Chat 硬超时已在真实作者副本通过；阶段 E 独立复审和公开 Preview 发布均已完成。
- Navigation 单入口的真实生成、局部 Diff、退出审阅零写入和取消已通过；Chat/Chapter/Research/普通 Changes/Graph/图片的作者路径、迟到结果隔离和零写入也已逐项验收。
- `@` 来源/实体候选已有 Main 目录和 request-bound ID 合同；真实项目候选、Main IPC 的 revision 漂移、foreign project identity、TTL 过期及 Chat 恢复 UI 已由 focused Electron 1/1 验证，五入口作者路径均使用同一 Context Manifest v2。
- `edit.md` 编译器已有跨入口实现；真实作者已对照 Chat/Navigation/Research/Chapter/Changes 的统一 revision、预算、遗漏/截断语义，并通过 Navigation/Chapter/Research/Changes 的旧结果丢弃；compile-invalid 不再回退发送原文。
- 阶段 E 已完成两条真实作者 Electron 证据：`author-cross-entry` 1/1 与 `author-affected` 1/1；覆盖 Chat → Navigation → Diff → 冲突阻止 → 接受 → Safe Undo，以及 Chapter、Research、普通 Changes、Graph、图片、五入口 edit.md revision 漂移、Chat 取消/60 秒超时、Navigation/Chapter/Research/Graph/图片取消、来源充足/不足“添加来源”双分支、添加来源后原 action identity 保留并回到 review、跨项目 A→B 迟到结果丢弃、A/B 零写入边界。Manifest 完整字段语义矩阵已由 `verify-v0-context-manifest.js` 4/4、服务专项和真实作者对照覆盖；独立复审已签收 P0=0、P1=0、P2=3，0.3.0 已进入公开 Developer Preview。

### 0.3.0 发布事实（2026-08-05）

- npm：`writ-craft@0.3.0` 已发布到 `preview`；registry 反查为 `preview: 0.3.0`、`latest: 0.1.0`。
- npm 完整性：shasum `c3294a3f106119096751f8c2b67afa55e91bd702`；公开 tarball 为 `https://registry.npmjs.org/writ-craft/-/writ-craft-0.3.0.tgz`。
- GitHub：`main`、annotated tag `v0.3.0` 与 prerelease 已生效；Tag/Release commit 为 `a747683`，Release 为 `https://github.com/MaxHou-infinity/WritCraft/releases/tag/v0.3.0`。
- 公网隔离安装验证：`WRITCRAFT_NPM_PREVIEW_PACKAGE_SPEC=writ-craft@preview` 的 `verify:npm-preview:installed` 通过 **2/2**，覆盖公开 tarball、Main `did-finish-load` IPC、profile 隔离、信号转发和清理；未打开作者稿件。
- 发布边界：这是 macOS npm Developer Preview；未移动 `latest`，未分发新的 App/ZIP，也不宣称稳定版。

### P0 / P1

- 0.3.0 独立复审 P0=0、P1=0；当前无工程阻断项。0.2.0 的 P0/P1 结论仅作为历史基线保存。

### 新增作者反馈：API Key 重启后似乎需要重输（2026-08-04，已关闭）

- 源码事实：`src/main/api-key-config-service.js` 已实现原子持久化、schema 校验和 `0600` 权限；`src/main/user-data-service.js` 默认将 profile 固定为 `~/Library/Application Support/WritCraft`，显式 `--profile` 则按设计隔离。
- 本机只读核对：稳定目录及两个历史目录均存在 `ai-config.json`，权限 `600`，Main `publicStatus` 均返回 `configured: true`；未读取或输出 Key 明文。
- 结论：固定同一启动方式与 profile 的真实 Electron 专项已验证保存后完全退出、重启、状态恢复及一次请求均成功；未读取或输出 Key 明文。输入框重启后为空是安全设计，不再视为丢失。

### 0.3.0 本轮真实 Electron 回归红灯（2026-08-04）

- 受影响 focused 路径已通过：项目卡真实 Electron 4/4、API Key 同 profile 1/1；AI 任务 7/7、进度 Renderer 3/3、Navigation 生产集成 10/10、Research Main 13/13 + 15/15 + 12/12、Research Renderer 13/13。
- 完整 `npm run e2e:electron` 首轮在前 12 个阶段通过后，于 Changes/History recovery 之后等待“一个统一 Writing Navigation 建议”超时；第二轮在更早的“普通 Markdown 可见回收区列表”等待处超时。两个红灯发生在不同阶段，且对应专项均全绿，当前归类为大型 Electron harness 的非固定时序 P2，不宣称完整 38/38 全绿。
- 完整 `npm test` 与获批 GUI 环境的 `npm run verify` 均通过；沙箱内 DOM sanitizer 的 `Electron exited null` 仅记录为环境红灯，原样转入非沙箱后通过。两条作者隔离副本脚本的独立真实验收已完成，不能把这两条 focused 绿灯扩写成大型 38 阶段 harness 永久稳定。
- 本轮完整 `npm run verify` 在获批 GUI 环境再次通过（退出码 0）；`npm run verify` 不包含完整 38 阶段 Electron harness，既有 `npm run e2e:electron` 不同时序红灯仍按 P2 保留，未用本次源码回归绿灯覆盖。

### 0.2.0 阶段 F 候选事实（2026-08-03）

- 当前会话 pending review 的真实 Electron 动态矩阵 3/3 通过：覆盖公开 `review_*` 身份 hydrate、丢弃、接受/拒绝、residual 新身份、旧身份失效、真实 TTL 过期、首页入口消失和正文零写入。Renderer 的 public ID 仅用于审阅决策，真实 ChangeSet capability 始终由 Main 解析。
- 作者授权源稿通过生产 copy transaction 创建隔离副本 `WritCraft-0.2.0-作者验收-1`：20 个 Markdown 文件、12 个正文文件；源摘要在复制前后完全一致。Computer Use 在独立 profile 中完成“首页查看状态 → 继续写作定位 → 查看当前文件大纲 → 返回首页”，无 AI、无付费调用，源稿和副本的非私有文件最终逐文件一致。
- 真实旅程发现并修复 P1：从首页打开最近文件时正文已经切换，但大纲仍描述旧文件。现在 Home 打开动作必须等待目标文件大纲刷新，再恢复正文定位；Computer Use 复验确认目标标题出现、旧大纲消失，返回首页后继续位置和 pending=0 正确。
- 最新源码 `npm test` 与非沙箱完整 `npm run verify` 通过；待审 focused Electron 3/3 通过。Daily Workspace focused 复跑再次在“外部文件 watcher 大纲刷新等待”处红灯；该非固定 harness 时序问题已保留为 P2，未用重跑绿灯覆盖。确定性 watcher/owner 专项、真实作者 UI 旅程和此前 focused 证据均通过，未形成稳定产品回归。
- 最终独立复审：P0=0、P1=0、P2=2，可进入候选。P2-1 为缺少“`refreshOutline` await 期间切项目”的 mutation-sensitive 专项，生产实现已有 refresh 后 owner 复核；P2-2 为通用 store 配置超过 10 项时 public projection 可能隐藏最旧映射，生产 Main 上限与 projection 同为 10，当前不可触发。两项均允许候选，未来触及对应代码或提高上限时必须先补合同和反例。
- 所有者于 2026-08-03 完成唯一一次最短主观旅程并明确回复“验收通过”。至此工程门禁、真实作者客观旅程和作者主观体验均已关闭，0.2.0 候选验收完成。随后已授权并完成 GitHub main 推送、`v0.2.0` Tag、GitHub prerelease 及 App/ZIP 上传；当时的 npm OTP 门禁是历史现场，0.2.0 不再单独发布，也没有待执行 OTP 动作。

### 0.2.0 发布执行记录（2026-08-03）

- 所有者已明确授权：执行 npm `preview`、GitHub 推送、`v0.2.0` Tag/Release，并上传 App/ZIP 产物。
- 发布前自动门禁：`npm test`、非沙箱 `npm run verify`、`npm run release:verify`、`npm audit --omit=dev` 均通过；npm tarball dry-run 为 `writ-craft-0.2.0.tgz`，140 个文件，683,229 bytes。
- 本地 macOS 产物：`v0/release/WritCraft-darwin-arm64.zip`；当前为 ad-hoc 本地签名、未公证，不宣称 Apple Developer ID 分发能力。
- 本节是 0.2.0 发布现场的历史记录；其能力已随 0.3.0 Preview 交付，不再等待 registry/dist-tag/提交补录，也不得从本节派发任何外部发布动作。

### 0.2.0 阶段 E 当前事实（2026-08-03）

- `writcraft.workspace/v2` 已实现并由 Main 独占迁移写权限：合法 v1 原子迁移；损坏、未来 schema 或失效路径安全降级且不覆盖原记录。保存/读取均绑定 exact `projectInstanceId`；保存 generation 由 preload 单调签发并由 Main 拒绝旧请求。
- 标签、当前文件、caret、selection、scroll、当前/折叠大纲和最多 32 项 `returnStack` 已持久化。返回时 revision 相同精确恢复，漂移时只恢复安全位置；临时 pending capability/location 不落盘。
- 项目首页、深跳与返回动作均绑定项目实例和 operation generation。真实 IPC 反例覆盖同项目旧异步保存落后于 close flush、保存执行中切换项目、旧项目 load/save、返回动作与首页 resolve/back 跨项目迟到；均不得覆盖新项目状态。
- 真实 Electron 性能复验：50 文件五次 `576.8 / 583.3 / 588.9 / 717.6 / 1169.4 ms`，max `1169.4 ms ≤ 1.5 s`；300 文件五次 `578.1 / 596.6 / 619.1 / 620.0 / 732.1 ms`，max `732.1 ms ≤ 3 s`。每次均为 fresh project/profile/process。
- 真实 `webContents.setZoomFactor(2)` 下 focused 工作区 2/2 通过，覆盖 Home → Continue → Return、`⌘P` 焦点/关闭、窄窗口键盘和项目切换 owner；不是用 deviceScaleFactor 冒充。
- `npm test` 与完整 `npm run verify` 通过；真实 Electron 曾完成 38/38 全绿。随后大型 harness 在不同旧路径出现三次非固定红灯（CDP evaluate、Chat fixture watcher 恢复、Recent return 等待），且曾在消除测试 sentinel 与真实滚动值冲突后再次 38/38 全绿；最后一次 owner 错误优先级修正后 focused zoom 2/2 通过，但全量复跑又在 Recent return 等待红灯。按纪律保留为 **P2 harness 时序不稳定**，不能用绿灯覆盖，也未发现稳定产品断言回归。
- 独立复审最终 P0=0、P1=0、P2=1。P2 为跨项目 in-flight 保存反例缺少 Main-side “delay 已进入” latch，当前依赖 Electron 同 sender IPC 顺序；实现本身已在 await 后重新取得当前项目并动态通过。阶段 F 若触及该 harness，应补 fixture-only latch，提高反例 mutation sensitivity。

### 0.2.0 阶段 D 当前事实（2026-08-03）

- 中央主工作区新增项目首页；左侧活动栏提供常驻入口。已有项目仍恢复上次编辑位置，新项目进入首页。首页只消费 Main `writcraft.project-home/v1` 快照，不在 Renderer 扫描正文或推断完成度。
- 首页包含继续写作、当前会话待审 Diff、最近修改、Graph/显式来源缺口和正文一览；所有入口都通过 Main stable locator 解析。revision 在 resolve→open 之间漂移时只重解一次，持续漂移明确失败；首页可响应 tree/current-file 权威变化重新加载。
- 新增 pending review 无 capability hydration：preload/Renderer 只传 public `reviewLocationId`，Main 负责 hydrate/apply/discard 并把 residual review 重新映射为新的 public location。过期、项目切换、迟到 hydration/discard 和 discard 失败均不污染新项目或伪报成功。
- 真实 Electron focused 2/2 通过，动态验证首页返回精确文件/光标、继续写作和最近修改卡片跳转；`npm run verify:daily-workspace`、Changes review UX 12/12、`npm test` 均通过。
- 完整 `npm run verify` 沙箱首跑在真实 DOM sanitizer 以 Electron `code=null` 退出；同一命令在获批非沙箱 GUI 环境原样复跑通过。误用旧环境变量的一次 38 阶段 harness 在首页阶段通过，随后于既有 Graph 冷重建时序断言红灯；保留为既有大型 harness P2，不归因于首页产品代码。
- 获批 GUI 环境曾完成一次完整 Electron 38/38；随后两次原样重跑分别在普通 Markdown 回收区列表和 Graph Issue undo recovery 超时。失败位置不同且专项回归全绿，按非固定时序 P2 保留，不能把一次 38/38 或后续 focused 绿灯写成完整 harness 永久稳定。
- 新增 `npm run e2e:electron:ai-task-progress`：真实 Electron Chat 入口 1/1 证明 Main 的准备/阶段/完成事件进入共享任务条，终态显示“已完成”；使用本地注入夹具，无真实网络和稿件外传。
- 独立复审先后发现 hydration owner、discard 失败误报、返回光标缺失和跨项目 discard 迟到污染等 P1，均已修复；最终 P0=0、P1=0。
- 阶段 D 当时保留的 pending 动态矩阵缺口已在阶段 F 以真实 Electron 3/3 关闭。超过 10 项 projection 的通用配置风险仍作为允许候选的 P2 保留；生产 Main 上限与 projection 同为 10，当前不可触发。

### 0.2.0 阶段 C 当前事实（2026-08-03）

- 新增独立 `writcraft.document-outline/v1` 投影：Main 公开当前文件 revision、稳定 section ID、层级/父级、occurrence、UTF-16 区间与不透明 locationId；最多 1,000 标题、512 KiB，最终 partial envelope 按完整字节预算。
- 左侧 Explorer 已加入“当前文稿”标题树；支持层级缩进、会话内折叠、树键盘操作与 revision-bound 当前区块高亮。折叠持久化明确留给阶段 E 的 workspace/v2 迁移，没有向 workspace/v1 静默写新字段。
- 顶层 `⌘P` 快速打开已接通 file/heading/entity/issue；结果按类型分组，保持同一 input 节点、焦点、active descendant，IME composition 期间不查询，项目切换与迟到查询/激活结果均由 owner 丢弃。
- `pending_review` 的 Main 公开投影与 resolve 仍保留，但 Renderer 快速打开暂不列出。原因是 Renderer reload 或同会话多份 review 需要 Stage D 的无 capability hydration；当前宁可不展示，也不把“Main 仍有效”误报成可打开或已过期。
- 激活同一未保存/冲突/已删除正文时 fail-closed；打开文件后 revision 漂移最多回 Main 重解一次。大纲不从 breadcrumb 猜层级，不建立第二套文件扫描。
- 已通过：`npm run verify:daily-workspace`、workspace 持久化 10/10、Changes review UX 12/12、完整 `npm test`、真实 Electron focused 2/2。真实 Electron 已验证大纲目标的 Main source offset、编辑器 caret offset 和 `aria-current` section ID 精确一致，以及 `⌘P` 输入节点/焦点/方向键选择。
- 真实 IME 首跑保留红灯：composition 期间，组合开始前的在途查询曾迟到更新状态。修复为 `compositionstart` 同步失效旧 request owner 后，真实 Electron 复跑 2/2 通过，并验证 input 节点、焦点和状态在 composition 中不重建，compositionend 后才查询。
- 独立复审首轮发现 512 KiB 尾部 +1 byte、激活 owner、workspace/v1 漂移、滚动/revision 高亮和 pending 假可用等 P1；已逐项修正或按阶段边界从 Stage C UI 移除。新增 H1→H3→H4→H2 父链、精确 512 KiB 与 +1 byte 回归；最终复审 P0=0、P1=0、P2=0。
- 阶段 C 已关闭：真实 Electron 已验证外部磁盘修改后正文与大纲同步刷新；720px 下大纲抽屉可由键盘进入、激活和退出并恢复焦点；300 文件冻结 inventory 的 100 次逐键式查询不触发第二套扫描并通过 1.5 秒测试上限。
- 最终复审曾发现外部 watcher 回调跨 await 切项目时可能污染新项目及旧恢复对话框的 P1。现以 `projectInstanceId + currentPath + sequence` owner 约束 tree/edit/read/recovery 全链，并在项目进入开始时同步作废旧 callback；复审关闭为 P0=0、P1=0、P2=0。
- 完整回归首跑因旧 `verify-v0-project-entry-workspace.js` 截取函数时漏带新增 owner 计数器而红灯；确认属于测试夹具漂移，校准夹具后最终源码 `npm test` 全绿。该红灯不得被后续绿色抹除。

### 0.2.0 阶段 B 核心服务（2026-08-03）

- 已实现 Main-only `workspace-inventory-service` 与独立 Worker runner：确定性 Markdown inventory、标题索引、正文计数、章节状态、扫描前后项目权威复核和 5 秒硬终止。
- 已实现 `project-home-snapshot-service`：统一 authority vector、最近文件/章节/待审/问题/来源区块、严格 Sources 状态、2 MiB 有界 partial 响应；Graph、Source、Pending generation 在扫描前后必须一致。
- 已实现 pending public projection 与 `workspace-location-service`：公开结果不携带 root/capability/revision，resolve 时重新核对当前项目、inventory、revision、offset 和待审身份；公开列表淘汰不会删除底层 ChangeSet。
- `npm run verify:daily-workspace` 全绿；原 pending store 回归 14/14；多轮独立复审最终 P0=0、P1=0。
- 保留 P2：无 `Intl.Segmenter` 环境的 emoji fallback、inventory 16 MiB 总读取上限的 partial 策略、Home 输入 exact-key/plain-object 加固、Workspace Location TTL/候选流式化及 2 MiB 精确 `+1 byte` fixture。它们不得被误写为已关闭。
- Main handler、IPC/preload 窄桥已接通：完整 Home/Graph/Source/mtime 聚合在同一 5 秒可终止 Worker 内完成；Location 可独立冷启动；Graph entity/issue 与 pending 均由 Main adapter 解析；IPC 返回稳定 public envelope。
- 真实 Electron focused harness 强制启动后 2/2 通过：真实 preload→IPC→Main 首页/位置成功、实际项目切换后的旧 location 拒绝、oversized 项目的 watcher 稳定失败，以及 public payload 无 root/capability 泄漏。
- 真实组合红灯发现并修复：合法 `Graph:Unicode-路径.md` 曾被上下文解析器误判为 URI，导致 inventory 整体失败；inventory 现只复用 Markdown 标题 grammar，并以真实项目相对路径独立生成稳定 `sec_` ID。Worker runner 只公开 allowlist code 与固定文案，不再转发 Worker 原始 message。
- 最终源码后 `npm run verify:daily-workspace`、`npm test`、focused Electron 2/2 全绿；独立复审 P0=0、P1=0。阶段 B 已关闭，但 Renderer 产品界面尚未实现，因此仍不能称为 0.2.0 App 功能可用。
- 保留 P2：两次 38 阶段全量 Electron harness 分别在旧 Changes recovery 后的 CDP evaluate、旧 Markdown Trash 列表等待处红灯；相同源码的 Daily Workspace focused 均通过，暂归类为大型 harness 时序不稳定，后续阶段 E 治理。真实 Worker crash 的 IPC 脱敏由注入测试覆盖，尚未为生产增加 E2E 故障后门。
- 阶段 0 已通过实现、专项/完整回归、真实 Electron 与独立复审；这只表示技术基线完成，不表示 0.2.0 用户功能完成、候选完成或已发布。

### 0.2.0 前置技术卫生

以下历史路线遗漏已在 0.2.0 阶段 0 关闭：

1. **Main → Renderer 反向依赖已归零**：`block-anchor`、`context-selection` 已迁入 `src/shared/`；Main/Renderer 共用同一 UMD 实现。新增解析后的静态依赖门禁、Node/浏览器行为等价测试、Renderer script 与 npm tarball 断言。
2. **旧 Project Plan 已物理移除**：删除 11 个旧 Main/Renderer/测试文件，清除 Changes/E2E/package 残留；Chapter 内部区块规划、普通 Changes 范围预检、Research、Graph Issue 和历史 metrics 兼容保持可用。
3. **Navigation 已统一为 changes-only**：每张建议只签发一个 `changes` capability；依据链接直接使用 Main locator；`needs_sources` 仍回到同一任务，独立 Research 仍从 Sources 单独启动。
4. **Navigation 最大组合边界已闭合**：最大 2000 字/4096-byte goal、4096 evidence enum、最大 Unicode 输出与精确 1 MiB provider request 在同一 fixture 通过；`+1 byte` 在 provider 前以 `NAVIGATION_PROMPT_TOO_LARGE` 拒绝。
5. **复用既有能力**：`⌘⇧F` 保持全文搜索，`⌘P` 只做快速导航；共用 Main 索引，禁止第二套文件扫描。workspace 已能恢复标签、当前文件、光标和滚动，0.2.0 只增加选区、返回路径和安全 schema 迁移。
6. **限定待审 Changes 真相**：当前 pending ChangeSet 是进程内状态。项目首页只展示当前会话可验证的待审项；不得暗示重启后仍持久存在。

### 已分流的历史 P2

- Navigation/AI metrics 仍沿用部分历史 `plan/*` 字段：安排到 `0.3.0` 透明 AI 协作统一迁移；旧持久化事件保持可读。
- 图片 alt/caption 独立编辑与已保留素材清理：0.4.0 当前明确排除，除非生效路线图另行批准；不得从本历史项重开 Image Trash。
- Research 准确率、图片价值样本和 10 名作者内测：属于 `1.0.0` Go/No-Go 证据，不是 0.2.0 功能门禁。
- Developer ID、签名、公证和 Gatekeeper：仅在未来选择独立 App 分发时启动。
- 同 UID 0700 reserve 微窗口及已记录 filesystem residual：属于明确接受的威胁模型残余，不得伪装成未完成 TODO。

## 5. 0.2.0 已批准范围

已批准的产品范围：

- 项目首页：最近工作、确定性项目摘要、当前会话待审 Changes、Graph 问题和已有来源诊断；
- 当前章节大纲：标题层级、当前位置跟随和准确跳转；
- `⌘P` 快速打开：文件、标题、实体、问题和当前会话待审对象；
- 可恢复导航：复用现有 workspace 状态，补选区、返回路径和失效定位降级；
- 自动化优先验收与一次最短作者主观旅程；
- 0.2.0 新增或实质修改代码的健壮性、关键注释、JSDoc/合同和反例测试。

明确不做：AI 任务中心、`@` 引用、统一 Prompt 编译、云/协作、多模型、Graph 重做、AI 语义搜索和大纲拖拽改写。

### 阶段 0 验证证据

- `npm test`：通过。
- `npm run verify`：沙箱内首次在真实 DOM sanitizer 以 Electron `code=null` 退出；相同探针在获批非沙箱 GUI 环境 13/13 通过，随后完整 `npm run verify` 通过。红灯保留为环境分类证据，不以绿色覆盖。
- `WRITCRAFT_E2E_FORCE=1 npm run e2e:electron`：真实 Electron 37/37 通过，含 Navigation → inline Diff、接受/Safe Undo、独立 Research 与零 Renderer 网络。
- 独立复审初次发现 P1=3、P2=1：精确 1 MiB 组合 fixture、依据链接真实点击、文档同步和 npm script 递归检查；均已补齐并定向复跑。最终复审 P0=0、P1=0。

### 阶段 A 合同证据

- 新增 `docs/DAILY-WORKSPACE-V1-CONTRACT.md`，冻结单一 Main 快照、生产 envelope、authority vector、正文/章节确定性统计、区块状态、Workspace Location list/resolve、pending public projection、stableLocator、workspace/v2、returnStack、5 秒降级和 50/300 文件性能口径。
- 只读源码审计确认：现有 tree/read revision、标题解析、Graph/issue、Sources、pending store 与 workspace/v1 可复用；字数、章节状态、pending summary、统一 locator、selection/return path 和 close flush 属于阶段 B–E 的真实实现缺口。
- 独立复审首轮 P0=0、P1=4、P2=2；后续复审继续发现生产 envelope、authority vector、临时 locationId 持久化和任意正文返回位置未闭合。全部在合同中修复；最终复审 P0=0、P1=0，最后一个 outline ID 边界 P2 也已补入冻结合同。
- 阶段 A 只修改合同/状态文档，未制作假数据 UI、未新增产品写入权限、未运行付费调用。

## 6. 候选交接与下一动作

1. 阶段 0 已完成并签收；进入阶段 A 前先核对现有 History、watcher、项目树、原生 helper、capability 与 recovery 实现，禁止重复造扫描器或恢复系统。
2. 按冻结合同分层建立 Main snapshot service/handler、窄 IPC、私有存储、比较、Markdown 选择性恢复和安全删除权威；先闭合 failure/state matrix 与最小可运行 checkpoint，再扩展 UI。
3. 阶段 A 必须重新完成相关专项、`npm test`、`npm run verify`、真实 Electron/Computer Use、独立复审和活动文档/Nowledge Mem 同步；阶段 0 与 0.3.0 证据只作兼容基线。
4. npm 版本、dist-tag、Tag/GitHub Release、远端 push 或 App/ZIP 分发均属于新的外部动作，必须由所有者另行明确授权。

## 7. 本轮文档治理结果

- 旧 Project Plan 合同、Phase A 长规格、0.1.x 完整状态账本、早期 PDCA 和 0.1.1 发布说明已移入 `docs/archive/`。
- `docs/INDEX.md` 成为文档选择入口；`docs/ARCHITECTURE.md` 接替 Phase A 中仍有效的工程边界。
- 0.1.2 作者验收、图片合同、首次使用指南、PRD、正式路线图和 `AGENTS.md` 的过时状态已校准或列入同批校准。
- 0.3.0 候选收口已同步 `docs/ROADMAP.md`、`docs/ROADMAP-0.3.0.md`、PRD、README、阶段 E 独立复审记录和 Nowledge Mem。
- 0.4.0 已获批为 `WRC-0.4.0-R1`；阶段 0 合同/基线冻结已由独立复审以 P0=0、P1=0、P2=4 签收，当前派工入口已切换到阶段 A。旧路线图、冻结合同和历史材料的派工权已在 `docs/INDEX.md` 明确撤销。
- `raw/` 与 `deliverables/` 只作研究/历史输入，不得派发当前任务。

## 8. 续作纪律

- 新会话只先读 `docs/ROADMAP.md`、本文件、`docs/INDEX.md` 指向的相关合同和 `v0/package.json`。
- 归档文档只能用于追溯“为什么”，不能回答“下一步做什么”。
- 不从未勾选的历史 checklist 派工；先确认本文件仍将其列为开放项。
- 红灯不能被绿色重跑抹除；当前状态只保留仍有决策价值的红灯，完整过程在归档账本。
- 关键里程碑同步本文件、相关合同与 Nowledge Mem；真实稿证据只保存内容无关字段。
