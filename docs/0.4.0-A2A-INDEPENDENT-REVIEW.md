# WritCraft 0.4.0 A2a 独立复审

> Review tree：`2b8149406121d1ccf35badfa72d34de762345cce`
> Review date：2026-09-11
> Scope：完整 A2a checkpoint（快照 create/list 的 Main handler + 窄 IPC + preload + Renderer 接线）
> Result：**NO-GO；P0=0、P1=1、P2=5**

本记录是一次完整 finding batch。它不签收 A2a，不解锁 A1c，也不把 focused、schema、组件或
单次 Electron 绿灯外推为 checkpoint 完成。修订后由同一 reviewer 定点确认。

复审绑定前已独立执行 `git rev-parse HEAD` 与 `git status --porcelain=v1`，确认工作树干净且
在全部证据运行前后未被修改（只读约束成立）。

## P1 finding（唯一阻断项）

**A2a 自己的 App 边界 E2E 门禁是不稳定的，并已真实红过一次。**

`tests/verify-v0-snapshot-create-list-electron.js` 在复审的**第一次**执行即失败：

```
AssertionError at tests/verify-v0-snapshot-create-list-electron.js:136
  actual   { error: true,  status: '本地快照私有目录无法安全使用。请确认项目仍可写后重试。' }
  expected { error: false, status: '本地快照已创建。' }
```

随后 7 次执行（3 次常规 + 3 次在 10 路 CPU 负载下 + 1 次插桩）**全部通过**。因此是
**间歇性红灯**，不是稳定红。

已定位机制：`renderer/project-home-view.js:440/456` 会触发**非 quiet** 的 `refresh()`；
一次非 quiet 的 `loadSnapshots`（`:156`）若在测试的 chmod 之后失败，会走到 `:172-173` 并把
状态文案覆盖掉（插桩运行观察到 create 窗口内确有并发的非 quiet
「正在读取已提交的本地快照…」）。即：**测试在断言 quiet-refresh 的状态保留语义时，没有等待
它自身的 view-change refresh 静默**，存在竞争窗口。

**为什么这构成 P1 而非 P2**：该测试是 A2a checkpoint 明确命名的 App 边界证据，且已注册进
0.4 清册（`stage A / lane snapshot-app / evidenceKind electron-e2e / requiresGui`）。
一个自己命名、被当作签收依据的门禁呈现非确定性，等于该 checkpoint 的证据不可靠——
这与"红灯藏在无人运行的脚本里"不同，它正是我们声称通过的那一项。

## P2 findings

1. `tests/verify-v0-snapshot-service-create.js` 的汇总行由 `${passed}/16 passed` 改为
   `${passed}/${passed} passed` —— 计数变为自指，静默跳过的用例不再可被发现（正是架构师
   在 `SIMPLIFICATION-PROPOSAL-2026-09-11.md` 中列为最高杠杆的那类缺陷）。
2. `tests/verify-v0-project-service.js` 相对 `HEAD^` **丢失了 `'use strict';`**（从较旧的分支
   文件带入）。违反仓库风格。
3. 新测试**不在任何聚合门禁内**：`--run stage-a-components` 以 `!requiresGui` 过滤，因此
   `current-components` 跳过它；`verify:full` 的 `e2e:electron` 也不要求任何 per-feature
   Electron 套件。它是**只能手动运行**的，没有任何自动化会捕捉它的红。这与既有
   author-affected / context-catalog / delivery-preflight 脚本同型，属既存模式，但新门禁不应
   沿用。
4. `confirmation` 是**由 preload 注入的常量**，不是 Main 侧的人类手势权威校验；任何 renderer
   脚本都可直接调用 create。因为是非破坏性且受 20 快照 / 2 GiB 上限约束，仅 P2。
5. `tests/verify-v0-project-home-snapshot.js` 新增断言是**源码文本正则 grep**
   （`assert.match(homeSource, ...)`），属弱证据；但为增量添加，未替换既有断言。

## 已确认为正当的部分（非"看起来还行"式通过）

复审明确说明它**尝试攻击以下各项但未能攻破**：

- **移植保真度：实质逐字移植，声明成立。** `snapshot-handler.js` 与
  `native/snapshot-storage-helper.c` 的 sha256 与分支 `8880709` **完全相同**（分别为
  `15636c3a…`、`e4adae7b…`）。逐文件差异：project-service 2 行（仅 `'use strict'`）、
  worker 4 行（仅注释）、home-view 8 行（全部是预期的 `state.project`→`readState()` 重构）、
  preload 4 行、main.js 150 行（**全部是 main 分支漂移**，无一行属于 snapshot 接线——
  移植的 snapshot 代码与分支逐字一致）、index.html 19 行（其余为 HEAD 的 dialog 漂移）。
  **不存在虚假的"byte-identical"声明。**
- **`uid` 缺失：refutation impossible。** `markdown-trash-worker.js:22-26` 的 `rootIdentity()`
  要求且仅产出 `{dev,ino,mode}`；`sameRoot():28-30` 只比较这三者；
  `snapshot-storage-worker.js:432-435` 与 `project-service.js:591-599` 只比较 dev/ino；
  `project.rootIdentity` 在 `main.js:870-875` 产生为原始 bigint `{dev,ino,mode}`，而
  schema 形状的 `{schema,dev,ino,uid,mode}` 只由 native 字段回复构建
  （`snapshot-storage-worker.js:424-430`）并在 `:450` 消化。`git grep .rootIdentity 8880709`
  显示分支自身从未 digest `project.rootIdentity`，也从未读取其 `.uid`。**无消费者读取 uid，
  无摘要改变。**
- **Renderer 权威：健全。** `preload.js:204-227` 只发送
  `{schema, projectInstanceId}` / `+confirmation` / `+taskId`；`snapshot-handler.js:26-31` 用
  `assertExactKeys` 拒绝未知/缺失键（多余键 ⇒ `INVALID_SCHEMA_KEYS`）。**不存在 path /
  content / output-path 字段**；home-view 无 `rootPath` / `snapshotManifestDigest` 引用。
- **零写入：断言真实、非空洞。** C 侧 `project_fd` 的用途为 bind/read，**唯一写入**是
  `I` 初始化时的 `mkdirat(root->project_fd, ".writcraft", 0700)`
  （`snapshot-storage-helper.c:899`）；所有 stage/bundle/control 写入都经
  `storage->control_fd/bundles_fd/quarantine_fd`。E2E 的 `assert.deepStrictEqual([...markdownBytes(root)], [...beforeMarkdown])`
  是对每个 `.md` 的真实内容比较，且在重启前后各执行一次。
- **Stage B 未被触碰。** 提交未触及 `sources-view.js` 或
  `verify-v0-delivery-preflight-ipc-boundary.js`；`list-delivery-snapshots`（`main.js:2448`）与
  `preload.listDeliverySnapshots`（`preload.js:320`）原样保留，新通道以**新增**方式并行存在。
  该边界测试实测 4/4 通过、exit 0。
- **登记与可达性：已登记且挂上 npm script**（`package.json:54
  e2e:electron:snapshot-create-list`，真实可执行）。缺陷是 P2-3 所述的"不在聚合门禁内"。

## Evidence boundary

`WRC_A1B_*` 不适用（本 checkpoint 是 A2a）。实测 exit 0：
`check-v0-0-4-test-registration.js --check`（223 scripts / 54 current / 41 Stage A / 2 GUI-only）、
`npm run verify:0.4:current-components`、`verify-v0-delivery-preflight-ipc-boundary.js` 4/4、
`verify-v0-package.js` 8/8（未被本变更集修改）。

`npm test` 仅在 `posttest` 的 `verify-v0-npm-preview.js:51` 失败，根因确认为环境：
本机 npm 12.0.2 下 `npm pack --dry-run --json` 返回以包名为键的**对象**而非单元素数组，
而 `engines.npm` 声明 `>=10 <12`。**与本变更集无关，且不得为适配本机而放宽该冻结门禁。**

## Required confirmation

修复必须至少覆盖：

1. 消除上述竞争：在该断言前等待自身 view-change refresh 静默，或让 quiet 断言容忍并发的
   非 quiet refresh；并以多次重复运行（含负载下）证明不再是间歇性。
2. 恢复 `verify-v0-snapshot-service-create.js` 的真实计数分母，并恢复
   `verify-v0-project-service.js` 的 `'use strict';`。
3. 决定新 Electron 门禁如何被自动化捕捉（挂入某个聚合门禁或明确记录为手动），不得让它成为
   又一个"登记但无人运行"的脚本。

只有同一 reviewer 定点确认 P0/P1 清零后，A2a 才可签收。

## Reviewer's 未验证项（原样保留）

checked-in universal native binary 的逐字节可重现性；A1b native storage 内部除 I-command /
capacity-guard 之外的余下约 2.8k 行；`npm test` 中段链路输出（仅确认抵达 posttest）；
未在第二台机器/OS 上验证；Electron E2E 仅在本机运行。
