# WritCraft 0.4.0 A1b 独立复审

> Review tree：`e24bd5167c8b73bbf9754d993e0eceec860c142d`
> Review date：2026-08-19
> Scope：完整 A1b mixed EXISTING + `ROLLBACK_CREATE` checkpoint
> Result：**NO-GO；P0=0、P1=5、P2=1**

本记录是一次完整 finding batch。它不签收 A1b，不解锁 A1c/A2，也不把 focused、schema、
native 或 direct-service 绿灯外推为 checkpoint 完成。修订后由同一 reviewer 对这些 finding
定点确认。

## P1 findings

1. **多 EXISTING 生产路径未实现**：native E/R 只对单项进入生产实现，合法多项固定返回
   `UNKNOWN`，而合同要求完整有序 EXISTING 子集和最多 300 项选择。
2. **V verify 未实现/未消费**：`verifyExisting` 无条件返回 `UNKNOWN`，Main 接受 E/R
   terminal 前没有 fresh V，缺少 journal CAS 后对 records、leaf、History 的独立复验。
3. **ROLLBACK_CREATE 缺少分阶段持久化**：Main 在一个调用栈内完成 Q/R/D/A，A 清理
   control/receipt/final 后才 CAS `ROLLED_BACK`；崩溃或响应丢失时 journal 无法恢复已删除真相。
4. **服务不消费 ROLLED_BACK**：真实 snapshot restore service 在 transaction 返回
   `ROLLED_BACK` 后仍进入 History mainline，最终把已证明零净写降级为 `UNKNOWN`/manual。
5. **EXISTING ACK 未清理私有记录**：native ACK 校验 final 后直接返回 `ACKED`，未删除
   control/apply/final；Main 仍发布 `ACK_COMMITTED` 并清到 IDLE。

## P2 finding

`37e67e7` 不能标为已被 main 完全取代：其中的 durable Q、ROLLED_BACK、ACK_COMMITTED
publication/CAS 顺序仍是 P1-3 的有效设计参考。该提交跨越旧 schema，只能定向适配，不能
整体合并或直接 cherry-pick。

## Evidence boundary

- registration：221 scripts / 52 current / 40 Stage A，exit 0。
- mixed production journey：6/6；snapshot restore service：42/42。
- native lifecycle：67/67；E-2b 组合 gate：89/89；rollback-create lifecycle：exit 0。
- `npm run verify:0.4:current-components`：40/40 Stage A 与 9/9 Stage B preflight，exit 0。
- `npm test` 在受限环境的首红为 Electron `code=null`；真实 Electron 权限重跑 exit 0。

这些结果证明现有组件和回归基线可运行，但部分测试把未实现路径预期为 `UNKNOWN`，不能反证
上述合同/生产边界 findings。

## Required confirmation

修复必须至少覆盖：多 EXISTING + MISSING 真实成功/回滚；E/R 后 fresh V；Q/D/A 每个
publication/CAS/response-loss 边界；service-level zero-write terminal；EXISTING ACK 的
exact cleanup 与 replacement-preserve。只有同一 reviewer 定点确认 P0/P1=0 后，A1b 才可签收。

---

# 定点确认（同一 reviewer，2026-09-11）

> Confirmation tree：`1f43b7fc…`（修复批次，即上文本批次所修的对象）
> 原 finding batch 绑定：`e24bd51`
> Result：**GO；P0=0、P1=0、P2=2**
>
> 这是 **A1b 代码级签核**。它不是 App 边界、Stage、candidate 或 release 签核。

## 为什么此前缺这一段（如实记录）

GO 结论在 `2ab928d` 中写入了 `0.4.0-A1B-EXISTING-STATE-MATRIX.md` 与
`v0/DEVELOPMENT-STATUS.md`，但**当时没有回写进本文件**，导致本文件头部仍显示 NO-GO、
`Review tree` 仍指向 `e24bd51`。这是本仓库"文档互相矛盾"类缺陷的一个真实实例，
由 2026-09-12 的清理审计（审计员 A，finding F1）发现并指出。现补齐。

## 绑定（由 reviewer 独立执行，非采信声明）

- `HEAD` = `57daa081734381ad92416aefca5eaed5ca06dfd1`；`git status --porcelain=v1` 为空；
- `git diff --stat 1f43b7f..HEAD -- v0/src v0/native v0/tests` = **EMPTY**；
  完整 `--name-status` 仅含 `docs/0.4.0-EXECUTION-PROTOCOL.md` 与 `v0/DEVELOPMENT-STATUS.md`（+9 行）。
  → 被审代码与 `1f43b7f` 逐字节相同。
- **已提交二进制与源码一致（reviewer 自加的检查）**：用仓库自带编译处方从被审源码重建
  `public-markdown-create-helper.c`，比对已提交产物的 fat-slice `__TEXT,__text` / `__const` /
  `__cstring`，**x86_64 与 arm64 均 0 差异字节**（sizes 152773/440/24145 与 125524/270/24209）。
  这堵死了"committed binary 可能是旧的"这一漏洞。

## 逐项 P1 裁定

| P1 | 裁定 | 关键证据 |
|---|---|---|
| 1 多 EXISTING | **CLOSED** | `native/public-markdown-create-helper.c:8267-8271` 批量分派；`existing_output_batch:7444` 有序聚合；`existing_reconcile_readonly:7814+` 有序 R。旧缺陷测试改为 `tests/verify-v0-public-markdown-native-lifecycle.js:1202` 的正向断言（COMMITTED + 有序 selectedIds + 2 项 receipt + 完整 R 复现），新增 `:1230` 单记录被替换不得部分成功 |
| 2 fresh V | **CLOSED**（符合冻结矩阵） | `changes-history-reconciliation-service.js:5398-5413` 在 CAS 后调 `lifecycle.verify` + `assertVerifyResult`，强制 `COMMITTED` 且 `terminalReceiptDigest` 相等；顺序 E 候选 → Main 复验 → journal CAS → fresh R → fresh V，正是矩阵 §4「只有存储的 publication 才能授权 fresh R/V」 |
| 3 分期 ROLLBACK_CREATE | **CLOSED** | journal `ROLLBACK_CREATE_ATTEMPT_PUBLICATION`（PREPARED 写前 latch）+ `ROLLBACK_CREATE_PUBLICATION`（QUARANTINED→ROLLED_BACK→ACK_COMMITTED）；权威转移 `changes-history-marker-journal-schema.js:2026-2042`；install/settlement CAS `:2043-2060`；state⟺phase `:1578-1588`；重启路由 `rollbackUncommittedExistingRestore:6203`（**绝不重放 E、绝不重放 Q**），入口拦截 `runExistingRestore:5158-5167` |
| 4 service 消费 ROLLED_BACK | **CLOSED** | `snapshot-restore-service.js:864-895` 在 `commitMissingRestoreHistory` **之前** `finish()`（→`classify:6770-6772`→`terminalizeRolledBackMarker:4418`）→ 校验 terminal/zero_write_error/ROLLED_BACK → `clear()`（`:7721-7727`→`clearRollbackCreateJournal:7521`）→ 返回 `zero_write_error`。该分支无法落入 History 主线 |
| 5 EXISTING ACK 精确清理 | **CLOSED** | `acknowledgeExistingRestore:6499+`：FINALIZED → `assertExistingCleanupPublished` → CAS `ACK_PREPARED/CLEANUP_ARMED` → native A → `assertExistingCleanupAbsent` → CAS `ACK_COMMITTED/REMOVED`；native `existing_ack_remove_record:8123-8169` 只移除精确索引身份、响应丢失幂等、保留 same-byte/new-inode 替换。退役的 item-less 面已删除，测试断言 `schema[retired]===undefined`，生产代码 **零调用者** |
| E4_FA 门禁 | **已恢复** | `WRC_A1B_E4_FA=1 … 69/69 exit 0`；`verify-v0-public-markdown-native-lifecycle.js:1872` 正向证明退役 wire fail-closed 且零删除 |

## 三项特别裁定

**(a) V≡R —— 符合冻结契约（所有者已于 2026-09-11 接受并记入状态矩阵）。**
`native/public-markdown-create-helper.c:9130-9134` 把 `R` 与 `V` 都分派给
`existing_reconcile_readonly:7814`，`existing_reconcile_header_shape:7948` 同时接受两者——
同一实现体，仅命令字不同。判定为符合矩阵，因为矩阵对 V 的定义是**证明义务 + 顺序**而非
"第二套实现"；该共享体做**活的**复验（重读公共叶、由新观测叶重建 record、重新捕获磁盘
identity 与存储绑定比对、检查残留名、经 `existing_execute_nonpublic_authority_valid:6438`
→`existing_execute_history_before_exact:6219-6264` 复验原始 History），且 Main 把 R 与 V
作为**两次独立调用**分别与存储 publication 做 digest 比对。
**明示保留**：它不提供第二实现，故继承 R 的任何盲点。

**(b) `rollback_delete_sealed`（`:5649-5674`，调用 `:5685`）——未发现错误 FINALIZED 的反例。**
返回 FINALIZED 需同时满足：held authority + `rollback_private_authority_exact`；每条 rollback
control/receipt record 字节与身份精确；每条 quarantine 叶 `NAME_ABSENT`；**公共 MISSING 叶
`NAME_ABSENT`（`:5663`）**；重建 final record `NAME_EXACT` 且捕获身份；`rollback_namespace_clean`
（`:5669`）。**缺席本身从不充分**；任何漂移回落严格删除路径并 fail-closed 到 UNKNOWN。
**既有严格路径 diff 为纯插入，无 `-` 行。** 残留但符合契约：D 中途崩溃 → UNKNOWN/manual（矩阵 §6 明定）。

**(c) 两个由表征测试翻转的边界测试 —— 是真实修复，非放宽断言。**
**git 中无可比对 diff**：那两个 `KNOWN DEFECT (P1)` 测试从未存在于任何提交
（`git log --all -S"KNOWN DEFECT"` 为空），只存在于未提交 WIP。按结果判定为真实收敛：
`tests/verify-v0-snapshot-restore-mixed-journey.js:1219` 与 `:1285` 是使用真实 transaction +
真实 native helper 的完整集成测试，带 CAS/响应丢失注入，断言正向终态。

## 断言削弱扫描 —— 未发现

全量 `v0/tests/**` diff（`1eacabf..HEAD`）：新增 `assert` 309 行 vs 删除 26 行，162 行删除逐条
检查，每处都有等强或更强的替代（魔法索引→具名索引表、`assertAckRequest`→
`schema[retired]===undefined` + fail-closed、E UNKNOWN 表征→COMMITTED + 有序聚合 + R 复现、
清后存在性→清前字节捕获 + 清后逐 basename 缺失）。无 `.only`/`.skip`/`todo`/`expectedFailure`/
exit-0 逃逸；无测试被删；legacy baseline 未改。`verify-v0-npm-preview.js` **未被修改**，
其失败是真实工具链越界（本机 npm 12 vs `engines <12`，`npm pack --dry-run --json` 返回对象而非数组）。

## P2

- **P2-1（文档准确性）**：`DEVELOPMENT-STATUS.md` 曾称 mixed journey 含 2 个 `KNOWN DEFECT` 表征
  测试（树中无此串）、E2B 写成 `89/89`（实际 `91/91`）、mixed-journey 注释仍把 D 重放缺陷叙述为
  存在。**已于 `73c3bc6` 修复**（8 删/8 增，100% 注释，非注释改动 = 0）。
- **P2-2（潜在 schema 松弛，生产不可达）**：rollback 的 phase⟺publication 双向条件只强制
  publication→phase。因 `snapshot-public-markdown-phase-schema.js:319` 允许
  `CREATED_RECEIPT → CREATE_ROLLBACK_QUARANTINED`，理论上一条 CAS 可在 `rollbackCreatePublication
  === null` 时推进阶段。服务端从不产生该状态，路由 fail-closed，**无法据此跳过 Q/D/A**。
  保留为可选加固。

## reviewer 未验证项（原样保留）

未做真实 Electron/App 运行（A1b 出口门禁要求的 production Main/native/filesystem 证据是在
finding 所指的集成层达成，未覆盖 GUI 边界）；`npm test` 完整链在本机无法跑完（A3 级证据未验证）；
未以活动同 UID 攻击者身份尝试击破 helper（矩阵 §7 明确的 P2 残留）。
