# WritCraft 0.4.0 A1b 独立复审

> Review tree：`e24bd5167c8b73bbf9754d993e0eceec860c142d`
> Review date：2026-08-19
> Scope：完整 A1b mixed EXISTING + `ROLLBACK_CREATE` checkpoint
> Result：**NO-GO；P0=0、P1=5、P2=1**

本记录是一次完整 finding batch。它不签收 A1b，不解锁 A1c/A2，也不把 focused、schema、
native 或 direct-service 绿灯外推为 checkpoint 完成。修订后由同一 reviewer 对这些 finding
定点确认。

## P1 findings

1. **多 EXISTING 生产路径未实现**：native E/R 只对单项进入生产实现，合法多项固定返回
   `UNKNOWN`，而合同要求完整有序 EXISTING 子集和最多 300 项选择。
2. **V verify 未实现/未消费**：`verifyExisting` 无条件返回 `UNKNOWN`，Main 接受 E/R
   terminal 前没有 fresh V，缺少 journal CAS 后对 records、leaf、History 的独立复验。
3. **ROLLBACK_CREATE 缺少分阶段持久化**：Main 在一个调用栈内完成 Q/R/D/A，A 清理
   control/receipt/final 后才 CAS `ROLLED_BACK`；崩溃或响应丢失时 journal 无法恢复已删除真相。
4. **服务不消费 ROLLED_BACK**：真实 snapshot restore service 在 transaction 返回
   `ROLLED_BACK` 后仍进入 History mainline，最终把已证明零净写降级为 `UNKNOWN`/manual。
5. **EXISTING ACK 未清理私有记录**：native ACK 校验 final 后直接返回 `ACKED`，未删除
   control/apply/final；Main 仍发布 `ACK_COMMITTED` 并清到 IDLE。

## P2 finding

`37e67e7` 不能标为已被 main 完全取代：其中的 durable Q、ROLLED_BACK、ACK_COMMITTED
publication/CAS 顺序仍是 P1-3 的有效设计参考。该提交跨越旧 schema，只能定向适配，不能
整体合并或直接 cherry-pick。

## Evidence boundary

- registration：221 scripts / 52 current / 40 Stage A，exit 0。
- mixed production journey：6/6；snapshot restore service：42/42。
- native lifecycle：67/67；E-2b 组合 gate：89/89；rollback-create lifecycle：exit 0。
- `npm run verify:0.4:current-components`：40/40 Stage A 与 9/9 Stage B preflight，exit 0。
- `npm test` 在受限环境的首红为 Electron `code=null`；真实 Electron 权限重跑 exit 0。

这些结果证明现有组件和回归基线可运行，但部分测试把未实现路径预期为 `UNKNOWN`，不能反证
上述合同/生产边界 findings。

## Required confirmation

修复必须至少覆盖：多 EXISTING + MISSING 真实成功/回滚；E/R 后 fresh V；Q/D/A 每个
publication/CAS/response-loss 边界；service-level zero-write terminal；EXISTING ACK 的
exact cleanup 与 replacement-preserve。只有同一 reviewer 定点确认 P0/P1=0 后，A1b 才可签收。
