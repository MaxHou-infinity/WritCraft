# 0.4.0 A1b / E-1 Canonical Parity Review Boundary

> Date: 2026-08-13
> Scope: Stage A / A1b / E-1 parity only
> Status: payload content review is P0=0, P1=0, P2=0; final hash/diff binding review is pending

## 1. Signed claim and explicit non-claims

This checkpoint signs only descriptor-exact parity between the JavaScript
formal EXISTING authority and the native test-mode serializers for one- and
two-item `K NAME`, `K CONTROL`, and `K APPLY` output. The native path remains
compiled behind `WRITCRAFT_TEST_EXISTING_CANONICAL`, performs no public or
private filesystem mutation, and still returns the production `UNKNOWN`
envelope.

It does **not** sign production E mutation, `COMMITTED` or `UNCOMMITTED`, R/V/F,
`ROLLBACK_CREATE`, mixed transaction wiring, A1b, Stage A, A1c, A2, A-to-B, a
0.4.0 candidate, or release authorization.

## 2. Frozen payload manifest

The following SHA-256 values are the complete E-1 payload. The review record
itself is outside the payload to avoid recursive self-hashing; the final
reviewer response binds this record's own hash.

```text
e899be4e535230a977cec8b265fda60e82f9c84874d0ef0b387c144f555edf64  docs/INDEX.md
93371c269ad9dcde4b91daa50f8b4d68a8f3ff036483e28fd134b603ed7c84ba  docs/0.4.0-A1B-EXISTING-STATE-MATRIX.md
08acc5be6797429d0ac435bd6be4b33034963f45ef69b619cc2869b36d39eebd  v0/DEVELOPMENT-STATUS.md
b2272b9005f3be1e345b6853029495dbdb0ba646d3e57efe0c13ff87b31bfdbf  v0/src/main/snapshot-existing-restore-native-schema.js
e6c1ce6be6a29748ef050b7f4ce0d000ac5cf76f43533e091de834846a4e0f9f  v0/native/public-markdown-create-helper.c
0506b0fda77c2ac641f8ac102d43c046addf94c3a8c7bad05f06dc2cdd3cb794  v0/tests/verify-v0-snapshot-existing-restore-native-schema.js
d00ace0b82188d178fc7cb18145409103e65aea27a0faf4c90ad1ebba2900d17  v0/tests/verify-v0-public-markdown-native-lifecycle.js
```

The complete concatenated binary diff binding is:

```text
sha256:12ce0faf51c1e3e3542b73f68823fc1a76fb9b1b50fea1bdd2fd0585e0c7992f
```

It is reproduced in this exact order by the tracked diff for `docs/INDEX.md`
and `v0/DEVELOPMENT-STATUS.md`, followed by `git diff --no-index --binary
/dev/null <file>` for the five untracked payload files in manifest order.

## 3. Preserved red and green evidence

The production first-red is intentionally retained:

```text
cd v0
node tests/verify-v0-public-markdown-native-lifecycle.js
```

It exits `1` at the real descriptor-held production E fixture with actual
`UNKNOWN` and expected `COMMITTED`. This is the E-2 production gap, not an E-1
failure.

The E-1 gates are:

```text
cd v0
node tests/verify-v0-snapshot-existing-restore-native-schema.js
env WRC_A1B_CANONICAL_ONLY=1 node tests/verify-v0-public-markdown-native-lifecycle.js
xcrun --sdk macosx clang -std=c11 -Wall -Wextra -Werror -Os -Wframe-larger-than=2097152 -mmacosx-version-min=11.0 -arch arm64 native/public-markdown-create-helper.c -o /tmp/writcraft-a1b-normal
xcrun --sdk macosx clang -std=c11 -Wall -Wextra -Werror -Os -Wframe-larger-than=2097152 -mmacosx-version-min=11.0 -arch arm64 -DWRITCRAFT_TEST_EXISTING_CANONICAL native/public-markdown-create-helper.c -o /tmp/writcraft-a1b-canonical
npm run verify:0.4:registration
git diff --check
```

Observed evidence: schema `19/19`, canonical-only lifecycle `66/66`, both strict
Clang builds exit `0`, registration classifies `219` scripts (`50` current,
`169` legacy; `38` Stage A current), and `git diff --check` has no output.

## 4. Independent content review

The independent reviewer froze the five implementation/schema/test/matrix
files before reading and verified the hashes again after all checks. The
reviewer confirmed:

- the test-only trailer consumes exactly the request item count, rejects
  malformed or duplicate after-leaf digests, and exists only behind the test
  macro;
- one- and two-item name/control/apply output is ordered and byte-for-byte
  equal to the JavaScript canonical bundle;
- the test-mode code has no `openat`, rename, unlink, fsync, production
  `COMMITTED`, or production wire change;
- the ordinary production E fixture remains the explicit `UNKNOWN !=
  COMMITTED` red.

The content-review result is **P0=0, P1=0, P2=0**. The final reviewer must now
recompute this seven-file manifest, the full diff digest, this record's hash,
and `git diff --check`. Only that response completes the reproducible E-1
boundary and permits E-2 to begin.
